/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.mavenagi.resources.commons.types;

import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonIgnoreProperties;
import com.fasterxml.jackson.annotation.JsonProperty;
import com.fasterxml.jackson.annotation.JsonSubTypes;
import com.fasterxml.jackson.annotation.JsonTypeInfo;
import com.fasterxml.jackson.annotation.JsonTypeName;
import com.fasterxml.jackson.annotation.JsonUnwrapped;
import com.fasterxml.jackson.annotation.JsonValue;
import java.util.Objects;
import java.util.Optional;

public final class ActionExecutionParamValue {
    private final Value value;

    @JsonCreator(mode = JsonCreator.Mode.DELEGATING)
    private ActionExecutionParamValue(Value value) {
        this.value = value;
    }

    public <T> T visit(Visitor<T> visitor) {
        return value.visit(visitor);
    }

    public static ActionExecutionParamValue primitive(Object value) {
        return new ActionExecutionParamValue(new PrimitiveValue(value));
    }

    public static ActionExecutionParamValue attachment(AttachmentResponse value) {
        return new ActionExecutionParamValue(new AttachmentValue(value));
    }

    public boolean isPrimitive() {
        return value instanceof PrimitiveValue;
    }

    public boolean isAttachment() {
        return value instanceof AttachmentValue;
    }

    public boolean _isUnknown() {
        return value instanceof _UnknownValue;
    }

    public Optional<Object> getPrimitive() {
        if (isPrimitive()) {
            return Optional.of(((PrimitiveValue) value).value);
        }
        return Optional.empty();
    }

    public Optional<AttachmentResponse> getAttachment() {
        if (isAttachment()) {
            return Optional.of(((AttachmentValue) value).value);
        }
        return Optional.empty();
    }

    public Optional<Object> _getUnknown() {
        if (_isUnknown()) {
            return Optional.of(((_UnknownValue) value).value);
        }
        return Optional.empty();
    }

    @JsonValue
    private Value getValue() {
        return this.value;
    }

    public interface Visitor<T> {
        T visitPrimitive(Object primitive);

        T visitAttachment(AttachmentResponse attachment);

        T _visitUnknown(Object unknownType);
    }

    @JsonTypeInfo(use = JsonTypeInfo.Id.NAME, property = "valueType", visible = true, defaultImpl = _UnknownValue.class)
    @JsonSubTypes({@JsonSubTypes.Type(PrimitiveValue.class), @JsonSubTypes.Type(AttachmentValue.class)})
    @JsonIgnoreProperties(ignoreUnknown = true)
    private interface Value {
        <T> T visit(Visitor<T> visitor);
    }

    @JsonTypeName("primitive")
    @JsonIgnoreProperties("valueType")
    private static final class PrimitiveValue implements Value {
        @JsonProperty("value")
        private Object value;

        @JsonCreator(mode = JsonCreator.Mode.PROPERTIES)
        private PrimitiveValue(@JsonProperty("value") Object value) {
            this.value = value;
        }

        @java.lang.Override
        public <T> T visit(Visitor<T> visitor) {
            return visitor.visitPrimitive(value);
        }

        @java.lang.Override
        public boolean equals(Object other) {
            if (this == other) return true;
            return other instanceof PrimitiveValue && equalTo((PrimitiveValue) other);
        }

        private boolean equalTo(PrimitiveValue other) {
            return value.equals(other.value);
        }

        @java.lang.Override
        public int hashCode() {
            return Objects.hash(this.value);
        }

        @java.lang.Override
        public String toString() {
            return "ActionExecutionParamValue{" + "value: " + value + "}";
        }
    }

    @JsonTypeName("attachment")
    @JsonIgnoreProperties("valueType")
    private static final class AttachmentValue implements Value {
        @JsonUnwrapped
        private AttachmentResponse value;

        @JsonCreator(mode = JsonCreator.Mode.PROPERTIES)
        private AttachmentValue() {}

        private AttachmentValue(AttachmentResponse value) {
            this.value = value;
        }

        @java.lang.Override
        public <T> T visit(Visitor<T> visitor) {
            return visitor.visitAttachment(value);
        }

        @java.lang.Override
        public boolean equals(Object other) {
            if (this == other) return true;
            return other instanceof AttachmentValue && equalTo((AttachmentValue) other);
        }

        private boolean equalTo(AttachmentValue other) {
            return value.equals(other.value);
        }

        @java.lang.Override
        public int hashCode() {
            return Objects.hash(this.value);
        }

        @java.lang.Override
        public String toString() {
            return "ActionExecutionParamValue{" + "value: " + value + "}";
        }
    }

    @JsonIgnoreProperties("valueType")
    private static final class _UnknownValue implements Value {
        private String type;

        @JsonValue
        private Object value;

        @JsonCreator(mode = JsonCreator.Mode.PROPERTIES)
        private _UnknownValue(@JsonProperty("value") Object value) {}

        @java.lang.Override
        public <T> T visit(Visitor<T> visitor) {
            return visitor._visitUnknown(value);
        }

        @java.lang.Override
        public boolean equals(Object other) {
            if (this == other) return true;
            return other instanceof _UnknownValue && equalTo((_UnknownValue) other);
        }

        private boolean equalTo(_UnknownValue other) {
            return type.equals(other.type) && value.equals(other.value);
        }

        @java.lang.Override
        public int hashCode() {
            return Objects.hash(this.type, this.value);
        }

        @java.lang.Override
        public String toString() {
            return "ActionExecutionParamValue{" + "type: " + type + ", value: " + value + "}";
        }
    }
}
