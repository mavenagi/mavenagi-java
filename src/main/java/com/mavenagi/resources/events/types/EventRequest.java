/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.mavenagi.resources.events.types;

import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonIgnoreProperties;
import com.fasterxml.jackson.annotation.JsonProperty;
import com.fasterxml.jackson.annotation.JsonSubTypes;
import com.fasterxml.jackson.annotation.JsonTypeInfo;
import com.fasterxml.jackson.annotation.JsonTypeName;
import com.fasterxml.jackson.annotation.JsonUnwrapped;
import com.fasterxml.jackson.annotation.JsonValue;
import java.util.Objects;
import java.util.Optional;

public final class EventRequest {
    private final Value value;

    @JsonCreator(mode = JsonCreator.Mode.DELEGATING)
    private EventRequest(Value value) {
        this.value = value;
    }

    public <T> T visit(Visitor<T> visitor) {
        return value.visit(visitor);
    }

    public static EventRequest userEvent(NovelUserEvent value) {
        return new EventRequest(new UserEventValue(value));
    }

    public static EventRequest systemEvent(NovelSystemEvent value) {
        return new EventRequest(new SystemEventValue(value));
    }

    public boolean isUserEvent() {
        return value instanceof UserEventValue;
    }

    public boolean isSystemEvent() {
        return value instanceof SystemEventValue;
    }

    public boolean _isUnknown() {
        return value instanceof _UnknownValue;
    }

    public Optional<NovelUserEvent> getUserEvent() {
        if (isUserEvent()) {
            return Optional.of(((UserEventValue) value).value);
        }
        return Optional.empty();
    }

    public Optional<NovelSystemEvent> getSystemEvent() {
        if (isSystemEvent()) {
            return Optional.of(((SystemEventValue) value).value);
        }
        return Optional.empty();
    }

    public Optional<Object> _getUnknown() {
        if (_isUnknown()) {
            return Optional.of(((_UnknownValue) value).value);
        }
        return Optional.empty();
    }

    @JsonValue
    private Value getValue() {
        return this.value;
    }

    public interface Visitor<T> {
        T visitUserEvent(NovelUserEvent userEvent);

        T visitSystemEvent(NovelSystemEvent systemEvent);

        T _visitUnknown(Object unknownType);
    }

    @JsonTypeInfo(use = JsonTypeInfo.Id.NAME, property = "eventType", visible = true, defaultImpl = _UnknownValue.class)
    @JsonSubTypes({@JsonSubTypes.Type(UserEventValue.class), @JsonSubTypes.Type(SystemEventValue.class)})
    @JsonIgnoreProperties(ignoreUnknown = true)
    private interface Value {
        <T> T visit(Visitor<T> visitor);
    }

    @JsonTypeName("userEvent")
    @JsonIgnoreProperties("eventType")
    private static final class UserEventValue implements Value {
        @JsonUnwrapped
        private NovelUserEvent value;

        @JsonCreator(mode = JsonCreator.Mode.PROPERTIES)
        private UserEventValue() {}

        private UserEventValue(NovelUserEvent value) {
            this.value = value;
        }

        @java.lang.Override
        public <T> T visit(Visitor<T> visitor) {
            return visitor.visitUserEvent(value);
        }

        @java.lang.Override
        public boolean equals(Object other) {
            if (this == other) return true;
            return other instanceof UserEventValue && equalTo((UserEventValue) other);
        }

        private boolean equalTo(UserEventValue other) {
            return value.equals(other.value);
        }

        @java.lang.Override
        public int hashCode() {
            return Objects.hash(this.value);
        }

        @java.lang.Override
        public String toString() {
            return "EventRequest{" + "value: " + value + "}";
        }
    }

    @JsonTypeName("systemEvent")
    @JsonIgnoreProperties("eventType")
    private static final class SystemEventValue implements Value {
        @JsonUnwrapped
        private NovelSystemEvent value;

        @JsonCreator(mode = JsonCreator.Mode.PROPERTIES)
        private SystemEventValue() {}

        private SystemEventValue(NovelSystemEvent value) {
            this.value = value;
        }

        @java.lang.Override
        public <T> T visit(Visitor<T> visitor) {
            return visitor.visitSystemEvent(value);
        }

        @java.lang.Override
        public boolean equals(Object other) {
            if (this == other) return true;
            return other instanceof SystemEventValue && equalTo((SystemEventValue) other);
        }

        private boolean equalTo(SystemEventValue other) {
            return value.equals(other.value);
        }

        @java.lang.Override
        public int hashCode() {
            return Objects.hash(this.value);
        }

        @java.lang.Override
        public String toString() {
            return "EventRequest{" + "value: " + value + "}";
        }
    }

    @JsonIgnoreProperties("eventType")
    private static final class _UnknownValue implements Value {
        private String type;

        @JsonValue
        private Object value;

        @JsonCreator(mode = JsonCreator.Mode.PROPERTIES)
        private _UnknownValue(@JsonProperty("value") Object value) {}

        @java.lang.Override
        public <T> T visit(Visitor<T> visitor) {
            return visitor._visitUnknown(value);
        }

        @java.lang.Override
        public boolean equals(Object other) {
            if (this == other) return true;
            return other instanceof _UnknownValue && equalTo((_UnknownValue) other);
        }

        private boolean equalTo(_UnknownValue other) {
            return type.equals(other.type) && value.equals(other.value);
        }

        @java.lang.Override
        public int hashCode() {
            return Objects.hash(this.type, this.value);
        }

        @java.lang.Override
        public String toString() {
            return "EventRequest{" + "type: " + type + ", value: " + value + "}";
        }
    }
}
