/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.mavenagi.resources.conversation.types;

import com.fasterxml.jackson.annotation.JsonAnyGetter;
import com.fasterxml.jackson.annotation.JsonAnySetter;
import com.fasterxml.jackson.annotation.JsonIgnoreProperties;
import com.fasterxml.jackson.annotation.JsonInclude;
import com.fasterxml.jackson.annotation.JsonProperty;
import com.fasterxml.jackson.annotation.JsonSetter;
import com.fasterxml.jackson.annotation.Nulls;
import com.fasterxml.jackson.databind.annotation.JsonDeserialize;
import com.mavenagi.core.ObjectMappers;
import com.mavenagi.resources.commons.types.IBaseCursorRequest;
import java.util.HashMap;
import java.util.Map;
import java.util.Objects;
import java.util.Optional;

@JsonInclude(JsonInclude.Include.NON_ABSENT)
@JsonDeserialize(builder = ConversationsCursorSearchRequest.Builder.class)
public final class ConversationsCursorSearchRequest implements IBaseCursorRequest {
    private final Optional<Integer> size;

    private final Optional<Boolean> sortDesc;

    private final Optional<String> cursor;

    private final Optional<ConversationFilter> filter;

    private final Map<String, Object> additionalProperties;

    private ConversationsCursorSearchRequest(
            Optional<Integer> size,
            Optional<Boolean> sortDesc,
            Optional<String> cursor,
            Optional<ConversationFilter> filter,
            Map<String, Object> additionalProperties) {
        this.size = size;
        this.sortDesc = sortDesc;
        this.cursor = cursor;
        this.filter = filter;
        this.additionalProperties = additionalProperties;
    }

    /**
     * @return The size of the page to return, defaults to 20. Max 200.
     */
    @JsonProperty("size")
    @java.lang.Override
    public Optional<Integer> getSize() {
        return size;
    }

    /**
     * @return Whether to sort descending, defaults to true
     */
    @JsonProperty("sortDesc")
    @java.lang.Override
    public Optional<Boolean> getSortDesc() {
        return sortDesc;
    }

    /**
     * @return Opaque cursor from the previous response's <code>nextCursor</code>, passed back unchanged. Omit it to
     * start a new traversal. Every other field must stay identical for the whole traversal;
     * changing one is rejected rather than silently restarting from the beginning. Cursors have
     * no expiry, but a cursor can still be rejected with a 400 if the server's signing key has
     * since been rotated out; if that happens, discard it and restart the traversal.
     */
    @JsonProperty("cursor")
    @java.lang.Override
    public Optional<String> getCursor() {
        return cursor;
    }

    @JsonProperty("filter")
    public Optional<ConversationFilter> getFilter() {
        return filter;
    }

    @java.lang.Override
    public boolean equals(Object other) {
        if (this == other) return true;
        return other instanceof ConversationsCursorSearchRequest && equalTo((ConversationsCursorSearchRequest) other);
    }

    @JsonAnyGetter
    public Map<String, Object> getAdditionalProperties() {
        return this.additionalProperties;
    }

    private boolean equalTo(ConversationsCursorSearchRequest other) {
        return size.equals(other.size)
                && sortDesc.equals(other.sortDesc)
                && cursor.equals(other.cursor)
                && filter.equals(other.filter);
    }

    @java.lang.Override
    public int hashCode() {
        return Objects.hash(this.size, this.sortDesc, this.cursor, this.filter);
    }

    @java.lang.Override
    public String toString() {
        return ObjectMappers.stringify(this);
    }

    public static Builder builder() {
        return new Builder();
    }

    @JsonIgnoreProperties(ignoreUnknown = true)
    public static final class Builder {
        private Optional<Integer> size = Optional.empty();

        private Optional<Boolean> sortDesc = Optional.empty();

        private Optional<String> cursor = Optional.empty();

        private Optional<ConversationFilter> filter = Optional.empty();

        @JsonAnySetter
        private Map<String, Object> additionalProperties = new HashMap<>();

        private Builder() {}

        public Builder from(ConversationsCursorSearchRequest other) {
            size(other.getSize());
            sortDesc(other.getSortDesc());
            cursor(other.getCursor());
            filter(other.getFilter());
            return this;
        }

        /**
         * <p>The size of the page to return, defaults to 20. Max 200.</p>
         */
        @JsonSetter(value = "size", nulls = Nulls.SKIP)
        public Builder size(Optional<Integer> size) {
            this.size = size;
            return this;
        }

        public Builder size(Integer size) {
            this.size = Optional.ofNullable(size);
            return this;
        }

        /**
         * <p>Whether to sort descending, defaults to true</p>
         */
        @JsonSetter(value = "sortDesc", nulls = Nulls.SKIP)
        public Builder sortDesc(Optional<Boolean> sortDesc) {
            this.sortDesc = sortDesc;
            return this;
        }

        public Builder sortDesc(Boolean sortDesc) {
            this.sortDesc = Optional.ofNullable(sortDesc);
            return this;
        }

        /**
         * <p>Opaque cursor from the previous response's <code>nextCursor</code>, passed back unchanged. Omit it to
         * start a new traversal. Every other field must stay identical for the whole traversal;
         * changing one is rejected rather than silently restarting from the beginning. Cursors have
         * no expiry, but a cursor can still be rejected with a 400 if the server's signing key has
         * since been rotated out; if that happens, discard it and restart the traversal.</p>
         */
        @JsonSetter(value = "cursor", nulls = Nulls.SKIP)
        public Builder cursor(Optional<String> cursor) {
            this.cursor = cursor;
            return this;
        }

        public Builder cursor(String cursor) {
            this.cursor = Optional.ofNullable(cursor);
            return this;
        }

        @JsonSetter(value = "filter", nulls = Nulls.SKIP)
        public Builder filter(Optional<ConversationFilter> filter) {
            this.filter = filter;
            return this;
        }

        public Builder filter(ConversationFilter filter) {
            this.filter = Optional.ofNullable(filter);
            return this;
        }

        public ConversationsCursorSearchRequest build() {
            return new ConversationsCursorSearchRequest(size, sortDesc, cursor, filter, additionalProperties);
        }
    }
}
