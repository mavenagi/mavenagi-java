/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.mavenagi.resources.conversation.types;

import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonValue;

public final class SimulationFilter {
    public static final SimulationFilter ALL = new SimulationFilter(Value.ALL, "ALL");

    public static final SimulationFilter NON_SIMULATIONS =
            new SimulationFilter(Value.NON_SIMULATIONS, "NON_SIMULATIONS");

    public static final SimulationFilter SIMULATIONS = new SimulationFilter(Value.SIMULATIONS, "SIMULATIONS");

    private final Value value;

    private final String string;

    SimulationFilter(Value value, String string) {
        this.value = value;
        this.string = string;
    }

    public Value getEnumValue() {
        return value;
    }

    @java.lang.Override
    @JsonValue
    public String toString() {
        return this.string;
    }

    @java.lang.Override
    public boolean equals(Object other) {
        return (this == other)
                || (other instanceof SimulationFilter && this.string.equals(((SimulationFilter) other).string));
    }

    @java.lang.Override
    public int hashCode() {
        return this.string.hashCode();
    }

    public <T> T visit(Visitor<T> visitor) {
        switch (value) {
            case ALL:
                return visitor.visitAll();
            case NON_SIMULATIONS:
                return visitor.visitNonSimulations();
            case SIMULATIONS:
                return visitor.visitSimulations();
            case UNKNOWN:
            default:
                return visitor.visitUnknown(string);
        }
    }

    @JsonCreator(mode = JsonCreator.Mode.DELEGATING)
    public static SimulationFilter valueOf(String value) {
        switch (value) {
            case "ALL":
                return ALL;
            case "NON_SIMULATIONS":
                return NON_SIMULATIONS;
            case "SIMULATIONS":
                return SIMULATIONS;
            default:
                return new SimulationFilter(Value.UNKNOWN, value);
        }
    }

    public enum Value {
        NON_SIMULATIONS,

        SIMULATIONS,

        ALL,

        UNKNOWN
    }

    public interface Visitor<T> {
        T visitNonSimulations();

        T visitSimulations();

        T visitAll();

        T visitUnknown(String unknownType);
    }
}
