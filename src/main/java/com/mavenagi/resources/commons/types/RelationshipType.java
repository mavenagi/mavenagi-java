/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.mavenagi.resources.commons.types;

import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonValue;

public final class RelationshipType {
    public static final RelationshipType SPAWN_TO = new RelationshipType(Value.SPAWN_TO, "SPAWN_TO");

    public static final RelationshipType SPAWN_FROM = new RelationshipType(Value.SPAWN_FROM, "SPAWN_FROM");

    private final Value value;

    private final String string;

    RelationshipType(Value value, String string) {
        this.value = value;
        this.string = string;
    }

    public Value getEnumValue() {
        return value;
    }

    @java.lang.Override
    @JsonValue
    public String toString() {
        return this.string;
    }

    @java.lang.Override
    public boolean equals(Object other) {
        return (this == other)
                || (other instanceof RelationshipType && this.string.equals(((RelationshipType) other).string));
    }

    @java.lang.Override
    public int hashCode() {
        return this.string.hashCode();
    }

    public <T> T visit(Visitor<T> visitor) {
        switch (value) {
            case SPAWN_TO:
                return visitor.visitSpawnTo();
            case SPAWN_FROM:
                return visitor.visitSpawnFrom();
            case UNKNOWN:
            default:
                return visitor.visitUnknown(string);
        }
    }

    @JsonCreator(mode = JsonCreator.Mode.DELEGATING)
    public static RelationshipType valueOf(String value) {
        switch (value) {
            case "SPAWN_TO":
                return SPAWN_TO;
            case "SPAWN_FROM":
                return SPAWN_FROM;
            default:
                return new RelationshipType(Value.UNKNOWN, value);
        }
    }

    public enum Value {
        SPAWN_FROM,

        SPAWN_TO,

        UNKNOWN
    }

    public interface Visitor<T> {
        T visitSpawnFrom();

        T visitSpawnTo();

        T visitUnknown(String unknownType);
    }
}
