/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.mavenagi.resources.charters.types;

import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonValue;

public final class CharterStatus {
    public static final CharterStatus DELETED = new CharterStatus(Value.DELETED, "DELETED");

    public static final CharterStatus ACTIVE = new CharterStatus(Value.ACTIVE, "ACTIVE");

    public static final CharterStatus INACTIVE = new CharterStatus(Value.INACTIVE, "INACTIVE");

    private final Value value;

    private final String string;

    CharterStatus(Value value, String string) {
        this.value = value;
        this.string = string;
    }

    public Value getEnumValue() {
        return value;
    }

    @java.lang.Override
    @JsonValue
    public String toString() {
        return this.string;
    }

    @java.lang.Override
    public boolean equals(Object other) {
        return (this == other)
                || (other instanceof CharterStatus && this.string.equals(((CharterStatus) other).string));
    }

    @java.lang.Override
    public int hashCode() {
        return this.string.hashCode();
    }

    public <T> T visit(Visitor<T> visitor) {
        switch (value) {
            case DELETED:
                return visitor.visitDeleted();
            case ACTIVE:
                return visitor.visitActive();
            case INACTIVE:
                return visitor.visitInactive();
            case UNKNOWN:
            default:
                return visitor.visitUnknown(string);
        }
    }

    @JsonCreator(mode = JsonCreator.Mode.DELEGATING)
    public static CharterStatus valueOf(String value) {
        switch (value) {
            case "DELETED":
                return DELETED;
            case "ACTIVE":
                return ACTIVE;
            case "INACTIVE":
                return INACTIVE;
            default:
                return new CharterStatus(Value.UNKNOWN, value);
        }
    }

    public enum Value {
        ACTIVE,

        INACTIVE,

        DELETED,

        UNKNOWN
    }

    public interface Visitor<T> {
        T visitActive();

        T visitInactive();

        T visitDeleted();

        T visitUnknown(String unknownType);
    }
}
