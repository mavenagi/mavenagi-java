/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.mavenagi.resources.analytics.types;

import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonIgnoreProperties;
import com.fasterxml.jackson.annotation.JsonProperty;
import com.fasterxml.jackson.annotation.JsonSubTypes;
import com.fasterxml.jackson.annotation.JsonTypeInfo;
import com.fasterxml.jackson.annotation.JsonTypeName;
import com.fasterxml.jackson.annotation.JsonUnwrapped;
import com.fasterxml.jackson.annotation.JsonValue;
import java.util.Objects;
import java.util.Optional;

public final class EventMetric {
    private final Value value;

    @JsonCreator(mode = JsonCreator.Mode.DELEGATING)
    private EventMetric(Value value) {
        this.value = value;
    }

    public <T> T visit(Visitor<T> visitor) {
        return value.visit(visitor);
    }

    public static EventMetric count(EventCount value) {
        return new EventMetric(new CountValue(value));
    }

    public static EventMetric distinctCount(EventDistinctCount value) {
        return new EventMetric(new DistinctCountValue(value));
    }

    public boolean isCount() {
        return value instanceof CountValue;
    }

    public boolean isDistinctCount() {
        return value instanceof DistinctCountValue;
    }

    public boolean _isUnknown() {
        return value instanceof _UnknownValue;
    }

    public Optional<EventCount> getCount() {
        if (isCount()) {
            return Optional.of(((CountValue) value).value);
        }
        return Optional.empty();
    }

    public Optional<EventDistinctCount> getDistinctCount() {
        if (isDistinctCount()) {
            return Optional.of(((DistinctCountValue) value).value);
        }
        return Optional.empty();
    }

    public Optional<Object> _getUnknown() {
        if (_isUnknown()) {
            return Optional.of(((_UnknownValue) value).value);
        }
        return Optional.empty();
    }

    @JsonValue
    private Value getValue() {
        return this.value;
    }

    public interface Visitor<T> {
        T visitCount(EventCount count);

        T visitDistinctCount(EventDistinctCount distinctCount);

        T _visitUnknown(Object unknownType);
    }

    @JsonTypeInfo(use = JsonTypeInfo.Id.NAME, property = "type", visible = true, defaultImpl = _UnknownValue.class)
    @JsonSubTypes({@JsonSubTypes.Type(CountValue.class), @JsonSubTypes.Type(DistinctCountValue.class)})
    @JsonIgnoreProperties(ignoreUnknown = true)
    private interface Value {
        <T> T visit(Visitor<T> visitor);
    }

    @JsonTypeName("count")
    @JsonIgnoreProperties("type")
    private static final class CountValue implements Value {
        @JsonUnwrapped
        private EventCount value;

        @JsonCreator(mode = JsonCreator.Mode.PROPERTIES)
        private CountValue() {}

        private CountValue(EventCount value) {
            this.value = value;
        }

        @java.lang.Override
        public <T> T visit(Visitor<T> visitor) {
            return visitor.visitCount(value);
        }

        @java.lang.Override
        public boolean equals(Object other) {
            if (this == other) return true;
            return other instanceof CountValue && equalTo((CountValue) other);
        }

        private boolean equalTo(CountValue other) {
            return value.equals(other.value);
        }

        @java.lang.Override
        public int hashCode() {
            return Objects.hash(this.value);
        }

        @java.lang.Override
        public String toString() {
            return "EventMetric{" + "value: " + value + "}";
        }
    }

    @JsonTypeName("distinctCount")
    @JsonIgnoreProperties("type")
    private static final class DistinctCountValue implements Value {
        @JsonUnwrapped
        private EventDistinctCount value;

        @JsonCreator(mode = JsonCreator.Mode.PROPERTIES)
        private DistinctCountValue() {}

        private DistinctCountValue(EventDistinctCount value) {
            this.value = value;
        }

        @java.lang.Override
        public <T> T visit(Visitor<T> visitor) {
            return visitor.visitDistinctCount(value);
        }

        @java.lang.Override
        public boolean equals(Object other) {
            if (this == other) return true;
            return other instanceof DistinctCountValue && equalTo((DistinctCountValue) other);
        }

        private boolean equalTo(DistinctCountValue other) {
            return value.equals(other.value);
        }

        @java.lang.Override
        public int hashCode() {
            return Objects.hash(this.value);
        }

        @java.lang.Override
        public String toString() {
            return "EventMetric{" + "value: " + value + "}";
        }
    }

    @JsonIgnoreProperties("type")
    private static final class _UnknownValue implements Value {
        private String type;

        @JsonValue
        private Object value;

        @JsonCreator(mode = JsonCreator.Mode.PROPERTIES)
        private _UnknownValue(@JsonProperty("value") Object value) {}

        @java.lang.Override
        public <T> T visit(Visitor<T> visitor) {
            return visitor._visitUnknown(value);
        }

        @java.lang.Override
        public boolean equals(Object other) {
            if (this == other) return true;
            return other instanceof _UnknownValue && equalTo((_UnknownValue) other);
        }

        private boolean equalTo(_UnknownValue other) {
            return type.equals(other.type) && value.equals(other.value);
        }

        @java.lang.Override
        public int hashCode() {
            return Objects.hash(this.type, this.value);
        }

        @java.lang.Override
        public String toString() {
            return "EventMetric{" + "type: " + type + ", value: " + value + "}";
        }
    }
}
