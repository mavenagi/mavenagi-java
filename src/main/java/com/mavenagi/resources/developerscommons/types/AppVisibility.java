/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.mavenagi.resources.developerscommons.types;

import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonValue;

public final class AppVisibility {
    public static final AppVisibility IN_DEVELOPMENT = new AppVisibility(Value.IN_DEVELOPMENT, "IN_DEVELOPMENT");

    public static final AppVisibility PUBLIC = new AppVisibility(Value.PUBLIC, "PUBLIC");

    public static final AppVisibility PRIVATE = new AppVisibility(Value.PRIVATE, "PRIVATE");

    private final Value value;

    private final String string;

    AppVisibility(Value value, String string) {
        this.value = value;
        this.string = string;
    }

    public Value getEnumValue() {
        return value;
    }

    @java.lang.Override
    @JsonValue
    public String toString() {
        return this.string;
    }

    @java.lang.Override
    public boolean equals(Object other) {
        return (this == other)
                || (other instanceof AppVisibility && this.string.equals(((AppVisibility) other).string));
    }

    @java.lang.Override
    public int hashCode() {
        return this.string.hashCode();
    }

    public <T> T visit(Visitor<T> visitor) {
        switch (value) {
            case IN_DEVELOPMENT:
                return visitor.visitInDevelopment();
            case PUBLIC:
                return visitor.visitPublic();
            case PRIVATE:
                return visitor.visitPrivate();
            case UNKNOWN:
            default:
                return visitor.visitUnknown(string);
        }
    }

    @JsonCreator(mode = JsonCreator.Mode.DELEGATING)
    public static AppVisibility valueOf(String value) {
        switch (value) {
            case "IN_DEVELOPMENT":
                return IN_DEVELOPMENT;
            case "PUBLIC":
                return PUBLIC;
            case "PRIVATE":
                return PRIVATE;
            default:
                return new AppVisibility(Value.UNKNOWN, value);
        }
    }

    public enum Value {
        IN_DEVELOPMENT,

        PRIVATE,

        PUBLIC,

        UNKNOWN
    }

    public interface Visitor<T> {
        T visitInDevelopment();

        T visitPrivate();

        T visitPublic();

        T visitUnknown(String unknownType);
    }
}
