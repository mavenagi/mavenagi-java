/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.mavenagi.resources.conversation.types;

import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonIgnoreProperties;
import com.fasterxml.jackson.annotation.JsonProperty;
import com.fasterxml.jackson.annotation.JsonSubTypes;
import com.fasterxml.jackson.annotation.JsonTypeInfo;
import com.fasterxml.jackson.annotation.JsonTypeName;
import com.fasterxml.jackson.annotation.JsonUnwrapped;
import com.fasterxml.jackson.annotation.JsonValue;
import java.util.Objects;
import java.util.Optional;

public final class StreamResponse {
    private final Value value;

    @JsonCreator(mode = JsonCreator.Mode.DELEGATING)
    private StreamResponse(Value value) {
        this.value = value;
    }

    public <T> T visit(Visitor<T> visitor) {
        return value.visit(visitor);
    }

    public static StreamResponse text(AskStreamTextEvent value) {
        return new StreamResponse(new TextValue(value));
    }

    public static StreamResponse action(AskStreamActionEvent value) {
        return new StreamResponse(new ActionValue(value));
    }

    public static StreamResponse chart(AskStreamChartEvent value) {
        return new StreamResponse(new ChartValue(value));
    }

    public static StreamResponse metadata(AskStreamMetadataEvent value) {
        return new StreamResponse(new MetadataValue(value));
    }

    public static StreamResponse start(AskStreamStartEvent value) {
        return new StreamResponse(new StartValue(value));
    }

    public static StreamResponse end(AskStreamEndEvent value) {
        return new StreamResponse(new EndValue(value));
    }

    public boolean isText() {
        return value instanceof TextValue;
    }

    public boolean isAction() {
        return value instanceof ActionValue;
    }

    public boolean isChart() {
        return value instanceof ChartValue;
    }

    public boolean isMetadata() {
        return value instanceof MetadataValue;
    }

    public boolean isStart() {
        return value instanceof StartValue;
    }

    public boolean isEnd() {
        return value instanceof EndValue;
    }

    public boolean _isUnknown() {
        return value instanceof _UnknownValue;
    }

    public Optional<AskStreamTextEvent> getText() {
        if (isText()) {
            return Optional.of(((TextValue) value).value);
        }
        return Optional.empty();
    }

    public Optional<AskStreamActionEvent> getAction() {
        if (isAction()) {
            return Optional.of(((ActionValue) value).value);
        }
        return Optional.empty();
    }

    public Optional<AskStreamChartEvent> getChart() {
        if (isChart()) {
            return Optional.of(((ChartValue) value).value);
        }
        return Optional.empty();
    }

    public Optional<AskStreamMetadataEvent> getMetadata() {
        if (isMetadata()) {
            return Optional.of(((MetadataValue) value).value);
        }
        return Optional.empty();
    }

    public Optional<AskStreamStartEvent> getStart() {
        if (isStart()) {
            return Optional.of(((StartValue) value).value);
        }
        return Optional.empty();
    }

    public Optional<AskStreamEndEvent> getEnd() {
        if (isEnd()) {
            return Optional.of(((EndValue) value).value);
        }
        return Optional.empty();
    }

    public Optional<Object> _getUnknown() {
        if (_isUnknown()) {
            return Optional.of(((_UnknownValue) value).value);
        }
        return Optional.empty();
    }

    @JsonValue
    private Value getValue() {
        return this.value;
    }

    public interface Visitor<T> {
        T visitText(AskStreamTextEvent text);

        T visitAction(AskStreamActionEvent action);

        T visitChart(AskStreamChartEvent chart);

        T visitMetadata(AskStreamMetadataEvent metadata);

        T visitStart(AskStreamStartEvent start);

        T visitEnd(AskStreamEndEvent end);

        T _visitUnknown(Object unknownType);
    }

    @JsonTypeInfo(use = JsonTypeInfo.Id.NAME, property = "eventType", visible = true, defaultImpl = _UnknownValue.class)
    @JsonSubTypes({
        @JsonSubTypes.Type(TextValue.class),
        @JsonSubTypes.Type(ActionValue.class),
        @JsonSubTypes.Type(ChartValue.class),
        @JsonSubTypes.Type(MetadataValue.class),
        @JsonSubTypes.Type(StartValue.class),
        @JsonSubTypes.Type(EndValue.class)
    })
    @JsonIgnoreProperties(ignoreUnknown = true)
    private interface Value {
        <T> T visit(Visitor<T> visitor);
    }

    @JsonTypeName("text")
    @JsonIgnoreProperties("eventType")
    private static final class TextValue implements Value {
        @JsonUnwrapped
        private AskStreamTextEvent value;

        @JsonCreator(mode = JsonCreator.Mode.PROPERTIES)
        private TextValue() {}

        private TextValue(AskStreamTextEvent value) {
            this.value = value;
        }

        @java.lang.Override
        public <T> T visit(Visitor<T> visitor) {
            return visitor.visitText(value);
        }

        @java.lang.Override
        public boolean equals(Object other) {
            if (this == other) return true;
            return other instanceof TextValue && equalTo((TextValue) other);
        }

        private boolean equalTo(TextValue other) {
            return value.equals(other.value);
        }

        @java.lang.Override
        public int hashCode() {
            return Objects.hash(this.value);
        }

        @java.lang.Override
        public String toString() {
            return "StreamResponse{" + "value: " + value + "}";
        }
    }

    @JsonTypeName("action")
    @JsonIgnoreProperties("eventType")
    private static final class ActionValue implements Value {
        @JsonUnwrapped
        private AskStreamActionEvent value;

        @JsonCreator(mode = JsonCreator.Mode.PROPERTIES)
        private ActionValue() {}

        private ActionValue(AskStreamActionEvent value) {
            this.value = value;
        }

        @java.lang.Override
        public <T> T visit(Visitor<T> visitor) {
            return visitor.visitAction(value);
        }

        @java.lang.Override
        public boolean equals(Object other) {
            if (this == other) return true;
            return other instanceof ActionValue && equalTo((ActionValue) other);
        }

        private boolean equalTo(ActionValue other) {
            return value.equals(other.value);
        }

        @java.lang.Override
        public int hashCode() {
            return Objects.hash(this.value);
        }

        @java.lang.Override
        public String toString() {
            return "StreamResponse{" + "value: " + value + "}";
        }
    }

    @JsonTypeName("chart")
    @JsonIgnoreProperties("eventType")
    private static final class ChartValue implements Value {
        @JsonUnwrapped
        private AskStreamChartEvent value;

        @JsonCreator(mode = JsonCreator.Mode.PROPERTIES)
        private ChartValue() {}

        private ChartValue(AskStreamChartEvent value) {
            this.value = value;
        }

        @java.lang.Override
        public <T> T visit(Visitor<T> visitor) {
            return visitor.visitChart(value);
        }

        @java.lang.Override
        public boolean equals(Object other) {
            if (this == other) return true;
            return other instanceof ChartValue && equalTo((ChartValue) other);
        }

        private boolean equalTo(ChartValue other) {
            return value.equals(other.value);
        }

        @java.lang.Override
        public int hashCode() {
            return Objects.hash(this.value);
        }

        @java.lang.Override
        public String toString() {
            return "StreamResponse{" + "value: " + value + "}";
        }
    }

    @JsonTypeName("metadata")
    @JsonIgnoreProperties("eventType")
    private static final class MetadataValue implements Value {
        @JsonUnwrapped
        private AskStreamMetadataEvent value;

        @JsonCreator(mode = JsonCreator.Mode.PROPERTIES)
        private MetadataValue() {}

        private MetadataValue(AskStreamMetadataEvent value) {
            this.value = value;
        }

        @java.lang.Override
        public <T> T visit(Visitor<T> visitor) {
            return visitor.visitMetadata(value);
        }

        @java.lang.Override
        public boolean equals(Object other) {
            if (this == other) return true;
            return other instanceof MetadataValue && equalTo((MetadataValue) other);
        }

        private boolean equalTo(MetadataValue other) {
            return value.equals(other.value);
        }

        @java.lang.Override
        public int hashCode() {
            return Objects.hash(this.value);
        }

        @java.lang.Override
        public String toString() {
            return "StreamResponse{" + "value: " + value + "}";
        }
    }

    @JsonTypeName("start")
    @JsonIgnoreProperties("eventType")
    private static final class StartValue implements Value {
        @JsonUnwrapped
        private AskStreamStartEvent value;

        @JsonCreator(mode = JsonCreator.Mode.PROPERTIES)
        private StartValue() {}

        private StartValue(AskStreamStartEvent value) {
            this.value = value;
        }

        @java.lang.Override
        public <T> T visit(Visitor<T> visitor) {
            return visitor.visitStart(value);
        }

        @java.lang.Override
        public boolean equals(Object other) {
            if (this == other) return true;
            return other instanceof StartValue && equalTo((StartValue) other);
        }

        private boolean equalTo(StartValue other) {
            return value.equals(other.value);
        }

        @java.lang.Override
        public int hashCode() {
            return Objects.hash(this.value);
        }

        @java.lang.Override
        public String toString() {
            return "StreamResponse{" + "value: " + value + "}";
        }
    }

    @JsonTypeName("end")
    @JsonIgnoreProperties("eventType")
    private static final class EndValue implements Value {
        @JsonUnwrapped
        private AskStreamEndEvent value;

        @JsonCreator(mode = JsonCreator.Mode.PROPERTIES)
        private EndValue() {}

        private EndValue(AskStreamEndEvent value) {
            this.value = value;
        }

        @java.lang.Override
        public <T> T visit(Visitor<T> visitor) {
            return visitor.visitEnd(value);
        }

        @java.lang.Override
        public boolean equals(Object other) {
            if (this == other) return true;
            return other instanceof EndValue && equalTo((EndValue) other);
        }

        private boolean equalTo(EndValue other) {
            return value.equals(other.value);
        }

        @java.lang.Override
        public int hashCode() {
            return Objects.hash(this.value);
        }

        @java.lang.Override
        public String toString() {
            return "StreamResponse{" + "value: " + value + "}";
        }
    }

    @JsonIgnoreProperties("eventType")
    private static final class _UnknownValue implements Value {
        private String type;

        @JsonValue
        private Object value;

        @JsonCreator(mode = JsonCreator.Mode.PROPERTIES)
        private _UnknownValue(@JsonProperty("value") Object value) {}

        @java.lang.Override
        public <T> T visit(Visitor<T> visitor) {
            return visitor._visitUnknown(value);
        }

        @java.lang.Override
        public boolean equals(Object other) {
            if (this == other) return true;
            return other instanceof _UnknownValue && equalTo((_UnknownValue) other);
        }

        private boolean equalTo(_UnknownValue other) {
            return type.equals(other.type) && value.equals(other.value);
        }

        @java.lang.Override
        public int hashCode() {
            return Objects.hash(this.type, this.value);
        }

        @java.lang.Override
        public String toString() {
            return "StreamResponse{" + "type: " + type + ", value: " + value + "}";
        }
    }
}
