/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.mavenagi.resources.commons.types;

import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonIgnoreProperties;
import com.fasterxml.jackson.annotation.JsonProperty;
import com.fasterxml.jackson.annotation.JsonSubTypes;
import com.fasterxml.jackson.annotation.JsonTypeInfo;
import com.fasterxml.jackson.annotation.JsonTypeName;
import com.fasterxml.jackson.annotation.JsonUnwrapped;
import com.fasterxml.jackson.annotation.JsonValue;
import java.util.Objects;
import java.util.Optional;

public final class NumericCondition {
    private final Value value;

    @JsonCreator(mode = JsonCreator.Mode.DELEGATING)
    private NumericCondition(Value value) {
        this.value = value;
    }

    public <T> T visit(Visitor<T> visitor) {
        return value.visit(visitor);
    }

    public static NumericCondition comparison(NumericComparisonCondition value) {
        return new NumericCondition(new ComparisonValue(value));
    }

    public static NumericCondition membership(NumericMembershipCondition value) {
        return new NumericCondition(new MembershipValue(value));
    }

    public boolean isComparison() {
        return value instanceof ComparisonValue;
    }

    public boolean isMembership() {
        return value instanceof MembershipValue;
    }

    public boolean _isUnknown() {
        return value instanceof _UnknownValue;
    }

    public Optional<NumericComparisonCondition> getComparison() {
        if (isComparison()) {
            return Optional.of(((ComparisonValue) value).value);
        }
        return Optional.empty();
    }

    public Optional<NumericMembershipCondition> getMembership() {
        if (isMembership()) {
            return Optional.of(((MembershipValue) value).value);
        }
        return Optional.empty();
    }

    public Optional<Object> _getUnknown() {
        if (_isUnknown()) {
            return Optional.of(((_UnknownValue) value).value);
        }
        return Optional.empty();
    }

    @JsonValue
    private Value getValue() {
        return this.value;
    }

    public interface Visitor<T> {
        T visitComparison(NumericComparisonCondition comparison);

        T visitMembership(NumericMembershipCondition membership);

        T _visitUnknown(Object unknownType);
    }

    @JsonTypeInfo(use = JsonTypeInfo.Id.NAME, property = "opType", visible = true, defaultImpl = _UnknownValue.class)
    @JsonSubTypes({@JsonSubTypes.Type(ComparisonValue.class), @JsonSubTypes.Type(MembershipValue.class)})
    @JsonIgnoreProperties(ignoreUnknown = true)
    private interface Value {
        <T> T visit(Visitor<T> visitor);
    }

    @JsonTypeName("comparison")
    @JsonIgnoreProperties("opType")
    private static final class ComparisonValue implements Value {
        @JsonUnwrapped
        private NumericComparisonCondition value;

        @JsonCreator(mode = JsonCreator.Mode.PROPERTIES)
        private ComparisonValue() {}

        private ComparisonValue(NumericComparisonCondition value) {
            this.value = value;
        }

        @java.lang.Override
        public <T> T visit(Visitor<T> visitor) {
            return visitor.visitComparison(value);
        }

        @java.lang.Override
        public boolean equals(Object other) {
            if (this == other) return true;
            return other instanceof ComparisonValue && equalTo((ComparisonValue) other);
        }

        private boolean equalTo(ComparisonValue other) {
            return value.equals(other.value);
        }

        @java.lang.Override
        public int hashCode() {
            return Objects.hash(this.value);
        }

        @java.lang.Override
        public String toString() {
            return "NumericCondition{" + "value: " + value + "}";
        }
    }

    @JsonTypeName("membership")
    @JsonIgnoreProperties("opType")
    private static final class MembershipValue implements Value {
        @JsonUnwrapped
        private NumericMembershipCondition value;

        @JsonCreator(mode = JsonCreator.Mode.PROPERTIES)
        private MembershipValue() {}

        private MembershipValue(NumericMembershipCondition value) {
            this.value = value;
        }

        @java.lang.Override
        public <T> T visit(Visitor<T> visitor) {
            return visitor.visitMembership(value);
        }

        @java.lang.Override
        public boolean equals(Object other) {
            if (this == other) return true;
            return other instanceof MembershipValue && equalTo((MembershipValue) other);
        }

        private boolean equalTo(MembershipValue other) {
            return value.equals(other.value);
        }

        @java.lang.Override
        public int hashCode() {
            return Objects.hash(this.value);
        }

        @java.lang.Override
        public String toString() {
            return "NumericCondition{" + "value: " + value + "}";
        }
    }

    @JsonIgnoreProperties("opType")
    private static final class _UnknownValue implements Value {
        private String type;

        @JsonValue
        private Object value;

        @JsonCreator(mode = JsonCreator.Mode.PROPERTIES)
        private _UnknownValue(@JsonProperty("value") Object value) {}

        @java.lang.Override
        public <T> T visit(Visitor<T> visitor) {
            return visitor._visitUnknown(value);
        }

        @java.lang.Override
        public boolean equals(Object other) {
            if (this == other) return true;
            return other instanceof _UnknownValue && equalTo((_UnknownValue) other);
        }

        private boolean equalTo(_UnknownValue other) {
            return type.equals(other.type) && value.equals(other.value);
        }

        @java.lang.Override
        public int hashCode() {
            return Objects.hash(this.type, this.value);
        }

        @java.lang.Override
        public String toString() {
            return "NumericCondition{" + "type: " + type + ", value: " + value + "}";
        }
    }
}
