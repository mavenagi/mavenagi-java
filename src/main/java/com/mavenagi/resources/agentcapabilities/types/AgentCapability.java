/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.mavenagi.resources.agentcapabilities.types;

import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonIgnoreProperties;
import com.fasterxml.jackson.annotation.JsonProperty;
import com.fasterxml.jackson.annotation.JsonSubTypes;
import com.fasterxml.jackson.annotation.JsonTypeInfo;
import com.fasterxml.jackson.annotation.JsonTypeName;
import com.fasterxml.jackson.annotation.JsonUnwrapped;
import com.fasterxml.jackson.annotation.JsonValue;
import java.util.Objects;
import java.util.Optional;

public final class AgentCapability {
    private final Value value;

    @JsonCreator(mode = JsonCreator.Mode.DELEGATING)
    private AgentCapability(Value value) {
        this.value = value;
    }

    public <T> T visit(Visitor<T> visitor) {
        return value.visit(visitor);
    }

    public static AgentCapability action(ActionCapability value) {
        return new AgentCapability(new ActionValue(value));
    }

    public static AgentCapability trigger(TriggerCapability value) {
        return new AgentCapability(new TriggerValue(value));
    }

    public boolean isAction() {
        return value instanceof ActionValue;
    }

    public boolean isTrigger() {
        return value instanceof TriggerValue;
    }

    public boolean _isUnknown() {
        return value instanceof _UnknownValue;
    }

    public Optional<ActionCapability> getAction() {
        if (isAction()) {
            return Optional.of(((ActionValue) value).value);
        }
        return Optional.empty();
    }

    public Optional<TriggerCapability> getTrigger() {
        if (isTrigger()) {
            return Optional.of(((TriggerValue) value).value);
        }
        return Optional.empty();
    }

    public Optional<Object> _getUnknown() {
        if (_isUnknown()) {
            return Optional.of(((_UnknownValue) value).value);
        }
        return Optional.empty();
    }

    @JsonValue
    private Value getValue() {
        return this.value;
    }

    public interface Visitor<T> {
        T visitAction(ActionCapability action);

        T visitTrigger(TriggerCapability trigger);

        T _visitUnknown(Object unknownType);
    }

    @JsonTypeInfo(use = JsonTypeInfo.Id.NAME, property = "type", visible = true, defaultImpl = _UnknownValue.class)
    @JsonSubTypes({@JsonSubTypes.Type(ActionValue.class), @JsonSubTypes.Type(TriggerValue.class)})
    @JsonIgnoreProperties(ignoreUnknown = true)
    private interface Value {
        <T> T visit(Visitor<T> visitor);
    }

    @JsonTypeName("ACTION")
    @JsonIgnoreProperties("type")
    private static final class ActionValue implements Value {
        @JsonUnwrapped
        private ActionCapability value;

        @JsonCreator(mode = JsonCreator.Mode.PROPERTIES)
        private ActionValue() {}

        private ActionValue(ActionCapability value) {
            this.value = value;
        }

        @java.lang.Override
        public <T> T visit(Visitor<T> visitor) {
            return visitor.visitAction(value);
        }

        @java.lang.Override
        public boolean equals(Object other) {
            if (this == other) return true;
            return other instanceof ActionValue && equalTo((ActionValue) other);
        }

        private boolean equalTo(ActionValue other) {
            return value.equals(other.value);
        }

        @java.lang.Override
        public int hashCode() {
            return Objects.hash(this.value);
        }

        @java.lang.Override
        public String toString() {
            return "AgentCapability{" + "value: " + value + "}";
        }
    }

    @JsonTypeName("TRIGGER")
    @JsonIgnoreProperties("type")
    private static final class TriggerValue implements Value {
        @JsonUnwrapped
        private TriggerCapability value;

        @JsonCreator(mode = JsonCreator.Mode.PROPERTIES)
        private TriggerValue() {}

        private TriggerValue(TriggerCapability value) {
            this.value = value;
        }

        @java.lang.Override
        public <T> T visit(Visitor<T> visitor) {
            return visitor.visitTrigger(value);
        }

        @java.lang.Override
        public boolean equals(Object other) {
            if (this == other) return true;
            return other instanceof TriggerValue && equalTo((TriggerValue) other);
        }

        private boolean equalTo(TriggerValue other) {
            return value.equals(other.value);
        }

        @java.lang.Override
        public int hashCode() {
            return Objects.hash(this.value);
        }

        @java.lang.Override
        public String toString() {
            return "AgentCapability{" + "value: " + value + "}";
        }
    }

    @JsonIgnoreProperties("type")
    private static final class _UnknownValue implements Value {
        private String type;

        @JsonValue
        private Object value;

        @JsonCreator(mode = JsonCreator.Mode.PROPERTIES)
        private _UnknownValue(@JsonProperty("value") Object value) {}

        @java.lang.Override
        public <T> T visit(Visitor<T> visitor) {
            return visitor._visitUnknown(value);
        }

        @java.lang.Override
        public boolean equals(Object other) {
            if (this == other) return true;
            return other instanceof _UnknownValue && equalTo((_UnknownValue) other);
        }

        private boolean equalTo(_UnknownValue other) {
            return type.equals(other.type) && value.equals(other.value);
        }

        @java.lang.Override
        public int hashCode() {
            return Objects.hash(this.type, this.value);
        }

        @java.lang.Override
        public String toString() {
            return "AgentCapability{" + "type: " + type + ", value: " + value + "}";
        }
    }
}
