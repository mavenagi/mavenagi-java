/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.mavenagi.resources.appdirectory.types;

import com.fasterxml.jackson.annotation.JsonAnyGetter;
import com.fasterxml.jackson.annotation.JsonAnySetter;
import com.fasterxml.jackson.annotation.JsonIgnoreProperties;
import com.fasterxml.jackson.annotation.JsonInclude;
import com.fasterxml.jackson.annotation.JsonProperty;
import com.fasterxml.jackson.annotation.JsonSetter;
import com.fasterxml.jackson.databind.annotation.JsonDeserialize;
import com.mavenagi.core.ObjectMappers;
import java.util.HashMap;
import java.util.Map;
import java.util.Objects;

@JsonInclude(JsonInclude.Include.NON_ABSENT)
@JsonDeserialize(builder = InstallDirectoryAppRequest.Builder.class)
public final class InstallDirectoryAppRequest {
    private final Object settings;

    private final Map<String, Object> additionalProperties;

    private InstallDirectoryAppRequest(Object settings, Map<String, Object> additionalProperties) {
        this.settings = settings;
        this.additionalProperties = additionalProperties;
    }

    /**
     * @return The app's configuration, as an object keyed by setting key.
     * <p>The keys and accepted values are defined by the app itself: fetch the app with
     * <code>get</code> and read its <code>settingsSchema</code>, where every entry carries a <code>key</code>, a <code>type</code>, and
     * whether it is <code>required</code>. Values are merged into the installation's existing settings:
     * keys you omit keep their stored value, and the server does not currently reject an unknown
     * key or require an entry the schema marks <code>required</code>. Sending an empty object installs an
     * app that declares no settings.</p>
     * <p>Sensitive entries are returned redacted by <code>get</code>, and sending one back unchanged here is a
     * no-op that keeps the stored value, so a settings object can be read, edited and written
     * back whole. See <code>MarketplaceAppDetail.settings</code> for what is redacted and how.</p>
     * <p>The value's JSON type follows the entry's <code>type</code>:</p>
     * <p>| Entry type | Value |
     * | --- | --- |
     * | <code>text</code>, <code>multiline</code>, <code>color</code>, <code>dropdown</code> | string |
     * | <code>number</code> | number |
     * | <code>checkbox</code>, <code>switch</code> | boolean |
     * | <code>array</code> | list of strings |
     * | <code>complexarray</code> | list of objects |
     * | <code>oneOf</code>, <code>jsonSchema</code> | object matching the entry's schema |
     * | <code>image</code> | omitted — see below |</p>
     * <p><code>image</code> entries are file settings. Do not send their bytes here: upload them first with
     * <code>getSettingUploadUrl</code> using the same setting key, and the stored file is picked up
     * automatically. <code>section</code> entries are layout only and take no value.</p>
     * <pre><code class="language-json">{
     *   &quot;settings&quot;: {
     *     &quot;acme.api.key&quot;: &quot;sk-live-...&quot;,
     *     &quot;acme.region&quot;: &quot;us-east-1&quot;,
     *     &quot;acme.sync.enabled&quot;: true,
     *     &quot;acme.retry.count&quot;: 3
     *   }
     * }
     * </code></pre>
     */
    @JsonProperty("settings")
    public Object getSettings() {
        return settings;
    }

    @java.lang.Override
    public boolean equals(Object other) {
        if (this == other) return true;
        return other instanceof InstallDirectoryAppRequest && equalTo((InstallDirectoryAppRequest) other);
    }

    @JsonAnyGetter
    public Map<String, Object> getAdditionalProperties() {
        return this.additionalProperties;
    }

    private boolean equalTo(InstallDirectoryAppRequest other) {
        return settings.equals(other.settings);
    }

    @java.lang.Override
    public int hashCode() {
        return Objects.hash(this.settings);
    }

    @java.lang.Override
    public String toString() {
        return ObjectMappers.stringify(this);
    }

    public static SettingsStage builder() {
        return new Builder();
    }

    public interface SettingsStage {
        /**
         * <p>The app's configuration, as an object keyed by setting key.</p>
         * <p>The keys and accepted values are defined by the app itself: fetch the app with
         * <code>get</code> and read its <code>settingsSchema</code>, where every entry carries a <code>key</code>, a <code>type</code>, and
         * whether it is <code>required</code>. Values are merged into the installation's existing settings:
         * keys you omit keep their stored value, and the server does not currently reject an unknown
         * key or require an entry the schema marks <code>required</code>. Sending an empty object installs an
         * app that declares no settings.</p>
         * <p>Sensitive entries are returned redacted by <code>get</code>, and sending one back unchanged here is a
         * no-op that keeps the stored value, so a settings object can be read, edited and written
         * back whole. See <code>MarketplaceAppDetail.settings</code> for what is redacted and how.</p>
         * <p>The value's JSON type follows the entry's <code>type</code>:</p>
         * <p>| Entry type | Value |
         * | --- | --- |
         * | <code>text</code>, <code>multiline</code>, <code>color</code>, <code>dropdown</code> | string |
         * | <code>number</code> | number |
         * | <code>checkbox</code>, <code>switch</code> | boolean |
         * | <code>array</code> | list of strings |
         * | <code>complexarray</code> | list of objects |
         * | <code>oneOf</code>, <code>jsonSchema</code> | object matching the entry's schema |
         * | <code>image</code> | omitted — see below |</p>
         * <p><code>image</code> entries are file settings. Do not send their bytes here: upload them first with
         * <code>getSettingUploadUrl</code> using the same setting key, and the stored file is picked up
         * automatically. <code>section</code> entries are layout only and take no value.</p>
         * <pre><code class="language-json">{
         *   &quot;settings&quot;: {
         *     &quot;acme.api.key&quot;: &quot;sk-live-...&quot;,
         *     &quot;acme.region&quot;: &quot;us-east-1&quot;,
         *     &quot;acme.sync.enabled&quot;: true,
         *     &quot;acme.retry.count&quot;: 3
         *   }
         * }
         * </code></pre>
         */
        _FinalStage settings(Object settings);

        Builder from(InstallDirectoryAppRequest other);
    }

    public interface _FinalStage {
        InstallDirectoryAppRequest build();
    }

    @JsonIgnoreProperties(ignoreUnknown = true)
    public static final class Builder implements SettingsStage, _FinalStage {
        private Object settings;

        @JsonAnySetter
        private Map<String, Object> additionalProperties = new HashMap<>();

        private Builder() {}

        @java.lang.Override
        public Builder from(InstallDirectoryAppRequest other) {
            settings(other.getSettings());
            return this;
        }

        /**
         * <p>The app's configuration, as an object keyed by setting key.</p>
         * <p>The keys and accepted values are defined by the app itself: fetch the app with
         * <code>get</code> and read its <code>settingsSchema</code>, where every entry carries a <code>key</code>, a <code>type</code>, and
         * whether it is <code>required</code>. Values are merged into the installation's existing settings:
         * keys you omit keep their stored value, and the server does not currently reject an unknown
         * key or require an entry the schema marks <code>required</code>. Sending an empty object installs an
         * app that declares no settings.</p>
         * <p>Sensitive entries are returned redacted by <code>get</code>, and sending one back unchanged here is a
         * no-op that keeps the stored value, so a settings object can be read, edited and written
         * back whole. See <code>MarketplaceAppDetail.settings</code> for what is redacted and how.</p>
         * <p>The value's JSON type follows the entry's <code>type</code>:</p>
         * <p>| Entry type | Value |
         * | --- | --- |
         * | <code>text</code>, <code>multiline</code>, <code>color</code>, <code>dropdown</code> | string |
         * | <code>number</code> | number |
         * | <code>checkbox</code>, <code>switch</code> | boolean |
         * | <code>array</code> | list of strings |
         * | <code>complexarray</code> | list of objects |
         * | <code>oneOf</code>, <code>jsonSchema</code> | object matching the entry's schema |
         * | <code>image</code> | omitted — see below |</p>
         * <p><code>image</code> entries are file settings. Do not send their bytes here: upload them first with
         * <code>getSettingUploadUrl</code> using the same setting key, and the stored file is picked up
         * automatically. <code>section</code> entries are layout only and take no value.</p>
         * <pre><code class="language-json">{
         *   &quot;settings&quot;: {
         *     &quot;acme.api.key&quot;: &quot;sk-live-...&quot;,
         *     &quot;acme.region&quot;: &quot;us-east-1&quot;,
         *     &quot;acme.sync.enabled&quot;: true,
         *     &quot;acme.retry.count&quot;: 3
         *   }
         * }
         * </code></pre>
         * <p>The app's configuration, as an object keyed by setting key.</p>
         * <p>The keys and accepted values are defined by the app itself: fetch the app with
         * <code>get</code> and read its <code>settingsSchema</code>, where every entry carries a <code>key</code>, a <code>type</code>, and
         * whether it is <code>required</code>. Values are merged into the installation's existing settings:
         * keys you omit keep their stored value, and the server does not currently reject an unknown
         * key or require an entry the schema marks <code>required</code>. Sending an empty object installs an
         * app that declares no settings.</p>
         * <p>Sensitive entries are returned redacted by <code>get</code>, and sending one back unchanged here is a
         * no-op that keeps the stored value, so a settings object can be read, edited and written
         * back whole. See <code>MarketplaceAppDetail.settings</code> for what is redacted and how.</p>
         * <p>The value's JSON type follows the entry's <code>type</code>:</p>
         * <p>| Entry type | Value |
         * | --- | --- |
         * | <code>text</code>, <code>multiline</code>, <code>color</code>, <code>dropdown</code> | string |
         * | <code>number</code> | number |
         * | <code>checkbox</code>, <code>switch</code> | boolean |
         * | <code>array</code> | list of strings |
         * | <code>complexarray</code> | list of objects |
         * | <code>oneOf</code>, <code>jsonSchema</code> | object matching the entry's schema |
         * | <code>image</code> | omitted — see below |</p>
         * <p><code>image</code> entries are file settings. Do not send their bytes here: upload them first with
         * <code>getSettingUploadUrl</code> using the same setting key, and the stored file is picked up
         * automatically. <code>section</code> entries are layout only and take no value.</p>
         * <pre><code class="language-json">{
         *   &quot;settings&quot;: {
         *     &quot;acme.api.key&quot;: &quot;sk-live-...&quot;,
         *     &quot;acme.region&quot;: &quot;us-east-1&quot;,
         *     &quot;acme.sync.enabled&quot;: true,
         *     &quot;acme.retry.count&quot;: 3
         *   }
         * }
         * </code></pre>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("settings")
        public _FinalStage settings(Object settings) {
            this.settings = settings;
            return this;
        }

        @java.lang.Override
        public InstallDirectoryAppRequest build() {
            return new InstallDirectoryAppRequest(settings, additionalProperties);
        }
    }
}
