/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.mavenagi.resources.developerscommons.types;

import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonValue;

public final class AppInstalled {
    public static final AppInstalled NOT_INSTALLED = new AppInstalled(Value.NOT_INSTALLED, "NOT_INSTALLED");

    public static final AppInstalled INSTALLED = new AppInstalled(Value.INSTALLED, "INSTALLED");

    private final Value value;

    private final String string;

    AppInstalled(Value value, String string) {
        this.value = value;
        this.string = string;
    }

    public Value getEnumValue() {
        return value;
    }

    @java.lang.Override
    @JsonValue
    public String toString() {
        return this.string;
    }

    @java.lang.Override
    public boolean equals(Object other) {
        return (this == other) || (other instanceof AppInstalled && this.string.equals(((AppInstalled) other).string));
    }

    @java.lang.Override
    public int hashCode() {
        return this.string.hashCode();
    }

    public <T> T visit(Visitor<T> visitor) {
        switch (value) {
            case NOT_INSTALLED:
                return visitor.visitNotInstalled();
            case INSTALLED:
                return visitor.visitInstalled();
            case UNKNOWN:
            default:
                return visitor.visitUnknown(string);
        }
    }

    @JsonCreator(mode = JsonCreator.Mode.DELEGATING)
    public static AppInstalled valueOf(String value) {
        switch (value) {
            case "NOT_INSTALLED":
                return NOT_INSTALLED;
            case "INSTALLED":
                return INSTALLED;
            default:
                return new AppInstalled(Value.UNKNOWN, value);
        }
    }

    public enum Value {
        INSTALLED,

        NOT_INSTALLED,

        UNKNOWN
    }

    public interface Visitor<T> {
        T visitInstalled();

        T visitNotInstalled();

        T visitUnknown(String unknownType);
    }
}
