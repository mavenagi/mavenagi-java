/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.mavenagi.resources.commons.types;

import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonIgnoreProperties;
import com.fasterxml.jackson.annotation.JsonProperty;
import com.fasterxml.jackson.annotation.JsonSubTypes;
import com.fasterxml.jackson.annotation.JsonTypeInfo;
import com.fasterxml.jackson.annotation.JsonTypeName;
import com.fasterxml.jackson.annotation.JsonUnwrapped;
import com.fasterxml.jackson.annotation.JsonValue;
import java.util.Objects;
import java.util.Optional;

public final class MetadataFilter {
    private final Value value;

    @JsonCreator(mode = JsonCreator.Mode.DELEGATING)
    private MetadataFilter(Value value) {
        this.value = value;
    }

    public <T> T visit(Visitor<T> visitor) {
        return value.visit(visitor);
    }

    public static MetadataFilter entry(MetadataEntryCondition value) {
        return new MetadataFilter(new EntryValue(value));
    }

    public static MetadataFilter group(MetadataFilterGroup value) {
        return new MetadataFilter(new GroupValue(value));
    }

    public boolean isEntry() {
        return value instanceof EntryValue;
    }

    public boolean isGroup() {
        return value instanceof GroupValue;
    }

    public boolean _isUnknown() {
        return value instanceof _UnknownValue;
    }

    public Optional<MetadataEntryCondition> getEntry() {
        if (isEntry()) {
            return Optional.of(((EntryValue) value).value);
        }
        return Optional.empty();
    }

    public Optional<MetadataFilterGroup> getGroup() {
        if (isGroup()) {
            return Optional.of(((GroupValue) value).value);
        }
        return Optional.empty();
    }

    public Optional<Object> _getUnknown() {
        if (_isUnknown()) {
            return Optional.of(((_UnknownValue) value).value);
        }
        return Optional.empty();
    }

    @JsonValue
    private Value getValue() {
        return this.value;
    }

    public interface Visitor<T> {
        T visitEntry(MetadataEntryCondition entry);

        T visitGroup(MetadataFilterGroup group);

        T _visitUnknown(Object unknownType);
    }

    @JsonTypeInfo(
            use = JsonTypeInfo.Id.NAME,
            property = "metadataFilterType",
            visible = true,
            defaultImpl = _UnknownValue.class)
    @JsonSubTypes({@JsonSubTypes.Type(EntryValue.class), @JsonSubTypes.Type(GroupValue.class)})
    @JsonIgnoreProperties(ignoreUnknown = true)
    private interface Value {
        <T> T visit(Visitor<T> visitor);
    }

    @JsonTypeName("entry")
    @JsonIgnoreProperties("metadataFilterType")
    private static final class EntryValue implements Value {
        @JsonUnwrapped
        private MetadataEntryCondition value;

        @JsonCreator(mode = JsonCreator.Mode.PROPERTIES)
        private EntryValue() {}

        private EntryValue(MetadataEntryCondition value) {
            this.value = value;
        }

        @java.lang.Override
        public <T> T visit(Visitor<T> visitor) {
            return visitor.visitEntry(value);
        }

        @java.lang.Override
        public boolean equals(Object other) {
            if (this == other) return true;
            return other instanceof EntryValue && equalTo((EntryValue) other);
        }

        private boolean equalTo(EntryValue other) {
            return value.equals(other.value);
        }

        @java.lang.Override
        public int hashCode() {
            return Objects.hash(this.value);
        }

        @java.lang.Override
        public String toString() {
            return "MetadataFilter{" + "value: " + value + "}";
        }
    }

    @JsonTypeName("group")
    @JsonIgnoreProperties("metadataFilterType")
    private static final class GroupValue implements Value {
        @JsonUnwrapped
        private MetadataFilterGroup value;

        @JsonCreator(mode = JsonCreator.Mode.PROPERTIES)
        private GroupValue() {}

        private GroupValue(MetadataFilterGroup value) {
            this.value = value;
        }

        @java.lang.Override
        public <T> T visit(Visitor<T> visitor) {
            return visitor.visitGroup(value);
        }

        @java.lang.Override
        public boolean equals(Object other) {
            if (this == other) return true;
            return other instanceof GroupValue && equalTo((GroupValue) other);
        }

        private boolean equalTo(GroupValue other) {
            return value.equals(other.value);
        }

        @java.lang.Override
        public int hashCode() {
            return Objects.hash(this.value);
        }

        @java.lang.Override
        public String toString() {
            return "MetadataFilter{" + "value: " + value + "}";
        }
    }

    @JsonIgnoreProperties("metadataFilterType")
    private static final class _UnknownValue implements Value {
        private String type;

        @JsonValue
        private Object value;

        @JsonCreator(mode = JsonCreator.Mode.PROPERTIES)
        private _UnknownValue(@JsonProperty("value") Object value) {}

        @java.lang.Override
        public <T> T visit(Visitor<T> visitor) {
            return visitor._visitUnknown(value);
        }

        @java.lang.Override
        public boolean equals(Object other) {
            if (this == other) return true;
            return other instanceof _UnknownValue && equalTo((_UnknownValue) other);
        }

        private boolean equalTo(_UnknownValue other) {
            return type.equals(other.type) && value.equals(other.value);
        }

        @java.lang.Override
        public int hashCode() {
            return Objects.hash(this.type, this.value);
        }

        @java.lang.Override
        public String toString() {
            return "MetadataFilter{" + "type: " + type + ", value: " + value + "}";
        }
    }
}
