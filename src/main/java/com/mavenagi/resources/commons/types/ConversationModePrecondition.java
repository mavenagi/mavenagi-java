/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.mavenagi.resources.commons.types;

import com.fasterxml.jackson.annotation.JsonAnyGetter;
import com.fasterxml.jackson.annotation.JsonAnySetter;
import com.fasterxml.jackson.annotation.JsonIgnoreProperties;
import com.fasterxml.jackson.annotation.JsonInclude;
import com.fasterxml.jackson.annotation.JsonProperty;
import com.fasterxml.jackson.annotation.JsonSetter;
import com.fasterxml.jackson.annotation.Nulls;
import com.fasterxml.jackson.databind.annotation.JsonDeserialize;
import com.mavenagi.core.ObjectMappers;
import java.util.HashMap;
import java.util.Map;
import java.util.Objects;
import java.util.Optional;
import org.jetbrains.annotations.NotNull;

@JsonInclude(JsonInclude.Include.NON_ABSENT)
@JsonDeserialize(builder = ConversationModePrecondition.Builder.class)
public final class ConversationModePrecondition implements IPreconditionBase {
    private final Optional<PreconditionOperator> operator;

    private final ConversationMode conversationMode;

    private final Map<String, Object> additionalProperties;

    private ConversationModePrecondition(
            Optional<PreconditionOperator> operator,
            ConversationMode conversationMode,
            Map<String, Object> additionalProperties) {
        this.operator = operator;
        this.conversationMode = conversationMode;
        this.additionalProperties = additionalProperties;
    }

    /**
     * @return Operator to apply to this precondition
     */
    @JsonProperty("operator")
    @java.lang.Override
    public Optional<PreconditionOperator> getOperator() {
        return operator;
    }

    /**
     * @return The mode the conversation must be running in for the precondition to be met. Omit the operator for an exact match, or set it to NOT to invert (NOT + VOICE is equivalent to TEXT). Any other operator is rejected with a 400.
     */
    @JsonProperty("conversationMode")
    public ConversationMode getConversationMode() {
        return conversationMode;
    }

    @java.lang.Override
    public boolean equals(Object other) {
        if (this == other) return true;
        return other instanceof ConversationModePrecondition && equalTo((ConversationModePrecondition) other);
    }

    @JsonAnyGetter
    public Map<String, Object> getAdditionalProperties() {
        return this.additionalProperties;
    }

    private boolean equalTo(ConversationModePrecondition other) {
        return operator.equals(other.operator) && conversationMode.equals(other.conversationMode);
    }

    @java.lang.Override
    public int hashCode() {
        return Objects.hash(this.operator, this.conversationMode);
    }

    @java.lang.Override
    public String toString() {
        return ObjectMappers.stringify(this);
    }

    public static ConversationModeStage builder() {
        return new Builder();
    }

    public interface ConversationModeStage {
        /**
         * <p>The mode the conversation must be running in for the precondition to be met. Omit the operator for an exact match, or set it to NOT to invert (NOT + VOICE is equivalent to TEXT). Any other operator is rejected with a 400.</p>
         */
        _FinalStage conversationMode(@NotNull ConversationMode conversationMode);

        Builder from(ConversationModePrecondition other);
    }

    public interface _FinalStage {
        ConversationModePrecondition build();

        /**
         * <p>Operator to apply to this precondition</p>
         */
        _FinalStage operator(Optional<PreconditionOperator> operator);

        _FinalStage operator(PreconditionOperator operator);
    }

    @JsonIgnoreProperties(ignoreUnknown = true)
    public static final class Builder implements ConversationModeStage, _FinalStage {
        private ConversationMode conversationMode;

        private Optional<PreconditionOperator> operator = Optional.empty();

        @JsonAnySetter
        private Map<String, Object> additionalProperties = new HashMap<>();

        private Builder() {}

        @java.lang.Override
        public Builder from(ConversationModePrecondition other) {
            operator(other.getOperator());
            conversationMode(other.getConversationMode());
            return this;
        }

        /**
         * <p>The mode the conversation must be running in for the precondition to be met. Omit the operator for an exact match, or set it to NOT to invert (NOT + VOICE is equivalent to TEXT). Any other operator is rejected with a 400.</p>
         * <p>The mode the conversation must be running in for the precondition to be met. Omit the operator for an exact match, or set it to NOT to invert (NOT + VOICE is equivalent to TEXT). Any other operator is rejected with a 400.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("conversationMode")
        public _FinalStage conversationMode(@NotNull ConversationMode conversationMode) {
            this.conversationMode = Objects.requireNonNull(conversationMode, "conversationMode must not be null");
            return this;
        }

        /**
         * <p>Operator to apply to this precondition</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage operator(PreconditionOperator operator) {
            this.operator = Optional.ofNullable(operator);
            return this;
        }

        /**
         * <p>Operator to apply to this precondition</p>
         */
        @java.lang.Override
        @JsonSetter(value = "operator", nulls = Nulls.SKIP)
        public _FinalStage operator(Optional<PreconditionOperator> operator) {
            this.operator = operator;
            return this;
        }

        @java.lang.Override
        public ConversationModePrecondition build() {
            return new ConversationModePrecondition(operator, conversationMode, additionalProperties);
        }
    }
}
