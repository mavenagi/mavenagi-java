/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.mavenagi.resources.intelligentfields;

import com.fasterxml.jackson.core.JsonProcessingException;
import com.mavenagi.core.ClientOptions;
import com.mavenagi.core.MavenAGIApiException;
import com.mavenagi.core.MavenAGIException;
import com.mavenagi.core.MavenAGIHttpResponse;
import com.mavenagi.core.MediaTypes;
import com.mavenagi.core.ObjectMappers;
import com.mavenagi.core.QueryStringMapper;
import com.mavenagi.core.RequestOptions;
import com.mavenagi.resources.commons.errors.BadRequestError;
import com.mavenagi.resources.commons.errors.NotFoundError;
import com.mavenagi.resources.commons.errors.PayloadTooLargeError;
import com.mavenagi.resources.commons.errors.ServerError;
import com.mavenagi.resources.commons.errors.TooManyRequestsError;
import com.mavenagi.resources.commons.types.ErrorMessage;
import com.mavenagi.resources.intelligentfields.requests.IntelligentFieldDeleteRequest;
import com.mavenagi.resources.intelligentfields.requests.IntelligentFieldGetRequest;
import com.mavenagi.resources.intelligentfields.requests.IntelligentFieldPatchRequest;
import com.mavenagi.resources.intelligentfields.types.IntelligentFieldDetailResponse;
import com.mavenagi.resources.intelligentfields.types.IntelligentFieldRequest;
import com.mavenagi.resources.intelligentfields.types.IntelligentFieldResponse;
import com.mavenagi.resources.intelligentfields.types.IntelligentFieldValueSearchRequest;
import com.mavenagi.resources.intelligentfields.types.IntelligentFieldValueSearchResponse;
import java.io.IOException;
import okhttp3.Headers;
import okhttp3.HttpUrl;
import okhttp3.MediaType;
import okhttp3.OkHttpClient;
import okhttp3.Request;
import okhttp3.RequestBody;
import okhttp3.Response;
import okhttp3.ResponseBody;

public class RawIntelligentFieldsClient {
    protected final ClientOptions clientOptions;

    public RawIntelligentFieldsClient(ClientOptions clientOptions) {
        this.clientOptions = clientOptions;
    }

    /**
     * Create a new intelligent field. Intelligent fields are used to store custom LLM-generated values on entities like conversations or events.
     */
    public MavenAGIHttpResponse<IntelligentFieldResponse> createOrUpdate(IntelligentFieldRequest request) {
        return createOrUpdate(request, null);
    }

    /**
     * Create a new intelligent field. Intelligent fields are used to store custom LLM-generated values on entities like conversations or events.
     */
    public MavenAGIHttpResponse<IntelligentFieldResponse> createOrUpdate(
            IntelligentFieldRequest request, RequestOptions requestOptions) {
        HttpUrl httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl())
                .newBuilder()
                .addPathSegments("v1/intelligent-fields")
                .build();
        RequestBody body;
        try {
            body = RequestBody.create(
                    ObjectMappers.JSON_MAPPER.writeValueAsBytes(request), MediaTypes.APPLICATION_JSON);
        } catch (JsonProcessingException e) {
            throw new MavenAGIException("Failed to serialize request", e);
        }
        Request okhttpRequest = new Request.Builder()
                .url(httpUrl)
                .method("PUT", body)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Content-Type", "application/json")
                .addHeader("Accept", "application/json")
                .build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        try (Response response = client.newCall(okhttpRequest).execute()) {
            ResponseBody responseBody = response.body();
            if (response.isSuccessful()) {
                return new MavenAGIHttpResponse<>(
                        ObjectMappers.JSON_MAPPER.readValue(responseBody.string(), IntelligentFieldResponse.class),
                        response);
            }
            String responseBodyString = responseBody != null ? responseBody.string() : "{}";
            try {
                switch (response.code()) {
                    case 400:
                        throw new BadRequestError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ErrorMessage.class), response);
                    case 404:
                        throw new NotFoundError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ErrorMessage.class), response);
                    case 413:
                        throw new PayloadTooLargeError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ErrorMessage.class), response);
                    case 429:
                        throw new TooManyRequestsError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ErrorMessage.class), response);
                    case 500:
                        throw new ServerError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ErrorMessage.class), response);
                }
            } catch (JsonProcessingException ignored) {
                // unable to map error response, throwing generic error
            }
            throw new MavenAGIApiException(
                    "Error with status code " + response.code(),
                    response.code(),
                    ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class),
                    response);
        } catch (IOException e) {
            throw new MavenAGIException("Network error executing HTTP request", e);
        }
    }

    /**
     * Get an intelligent field by its supplied ID
     */
    public MavenAGIHttpResponse<IntelligentFieldDetailResponse> get(String fieldReferenceId) {
        return get(fieldReferenceId, IntelligentFieldGetRequest.builder().build());
    }

    /**
     * Get an intelligent field by its supplied ID
     */
    public MavenAGIHttpResponse<IntelligentFieldDetailResponse> get(
            String fieldReferenceId, IntelligentFieldGetRequest request) {
        return get(fieldReferenceId, request, null);
    }

    /**
     * Get an intelligent field by its supplied ID
     */
    public MavenAGIHttpResponse<IntelligentFieldDetailResponse> get(
            String fieldReferenceId, IntelligentFieldGetRequest request, RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl())
                .newBuilder()
                .addPathSegments("v1/intelligent-fields")
                .addPathSegment(fieldReferenceId);
        if (request.getAppId().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "appId", request.getAppId().get(), false);
        }
        Request.Builder _requestBuilder = new Request.Builder()
                .url(httpUrl.build())
                .method("GET", null)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Accept", "application/json");
        Request okhttpRequest = _requestBuilder.build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        try (Response response = client.newCall(okhttpRequest).execute()) {
            ResponseBody responseBody = response.body();
            if (response.isSuccessful()) {
                return new MavenAGIHttpResponse<>(
                        ObjectMappers.JSON_MAPPER.readValue(
                                responseBody.string(), IntelligentFieldDetailResponse.class),
                        response);
            }
            String responseBodyString = responseBody != null ? responseBody.string() : "{}";
            try {
                switch (response.code()) {
                    case 400:
                        throw new BadRequestError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ErrorMessage.class), response);
                    case 404:
                        throw new NotFoundError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ErrorMessage.class), response);
                    case 413:
                        throw new PayloadTooLargeError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ErrorMessage.class), response);
                    case 429:
                        throw new TooManyRequestsError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ErrorMessage.class), response);
                    case 500:
                        throw new ServerError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ErrorMessage.class), response);
                }
            } catch (JsonProcessingException ignored) {
                // unable to map error response, throwing generic error
            }
            throw new MavenAGIApiException(
                    "Error with status code " + response.code(),
                    response.code(),
                    ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class),
                    response);
        } catch (IOException e) {
            throw new MavenAGIException("Network error executing HTTP request", e);
        }
    }

    /**
     * Patch an intelligent field. Can be used to update the definition, status, or other mutable properties.
     */
    public MavenAGIHttpResponse<IntelligentFieldResponse> patch(String fieldReferenceId) {
        return patch(fieldReferenceId, IntelligentFieldPatchRequest.builder().build());
    }

    /**
     * Patch an intelligent field. Can be used to update the definition, status, or other mutable properties.
     */
    public MavenAGIHttpResponse<IntelligentFieldResponse> patch(
            String fieldReferenceId, IntelligentFieldPatchRequest request) {
        return patch(fieldReferenceId, request, null);
    }

    /**
     * Patch an intelligent field. Can be used to update the definition, status, or other mutable properties.
     */
    public MavenAGIHttpResponse<IntelligentFieldResponse> patch(
            String fieldReferenceId, IntelligentFieldPatchRequest request, RequestOptions requestOptions) {
        HttpUrl httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl())
                .newBuilder()
                .addPathSegments("v1/intelligent-fields")
                .addPathSegment(fieldReferenceId)
                .build();
        RequestBody body;
        try {
            body = RequestBody.create(
                    ObjectMappers.JSON_MAPPER.writeValueAsBytes(request),
                    MediaType.parse("application/merge-patch+json"));
        } catch (JsonProcessingException e) {
            throw new MavenAGIException("Failed to serialize request", e);
        }
        Request okhttpRequest = new Request.Builder()
                .url(httpUrl)
                .method("PATCH", body)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Content-Type", "application/merge-patch+json")
                .addHeader("Accept", "application/json")
                .build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        try (Response response = client.newCall(okhttpRequest).execute()) {
            ResponseBody responseBody = response.body();
            if (response.isSuccessful()) {
                return new MavenAGIHttpResponse<>(
                        ObjectMappers.JSON_MAPPER.readValue(responseBody.string(), IntelligentFieldResponse.class),
                        response);
            }
            String responseBodyString = responseBody != null ? responseBody.string() : "{}";
            try {
                switch (response.code()) {
                    case 400:
                        throw new BadRequestError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ErrorMessage.class), response);
                    case 404:
                        throw new NotFoundError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ErrorMessage.class), response);
                    case 413:
                        throw new PayloadTooLargeError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ErrorMessage.class), response);
                    case 429:
                        throw new TooManyRequestsError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ErrorMessage.class), response);
                    case 500:
                        throw new ServerError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ErrorMessage.class), response);
                }
            } catch (JsonProcessingException ignored) {
                // unable to map error response, throwing generic error
            }
            throw new MavenAGIApiException(
                    "Error with status code " + response.code(),
                    response.code(),
                    ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class),
                    response);
        } catch (IOException e) {
            throw new MavenAGIException("Network error executing HTTP request", e);
        }
    }

    /**
     * Soft delete an intelligent field. Only INACTIVE fields can be deleted.
     * <p>Deleted fields are excluded from search results but can still be retrieved by ID.
     * Creating a new field with the same referenceId as a deleted field will overwrite
     * the deleted field and restore it to INACTIVE status.</p>
     * <p>Deleted fields cannot be modified.</p>
     */
    public MavenAGIHttpResponse<IntelligentFieldResponse> delete(String fieldReferenceId) {
        return delete(fieldReferenceId, IntelligentFieldDeleteRequest.builder().build());
    }

    /**
     * Soft delete an intelligent field. Only INACTIVE fields can be deleted.
     * <p>Deleted fields are excluded from search results but can still be retrieved by ID.
     * Creating a new field with the same referenceId as a deleted field will overwrite
     * the deleted field and restore it to INACTIVE status.</p>
     * <p>Deleted fields cannot be modified.</p>
     */
    public MavenAGIHttpResponse<IntelligentFieldResponse> delete(
            String fieldReferenceId, IntelligentFieldDeleteRequest request) {
        return delete(fieldReferenceId, request, null);
    }

    /**
     * Soft delete an intelligent field. Only INACTIVE fields can be deleted.
     * <p>Deleted fields are excluded from search results but can still be retrieved by ID.
     * Creating a new field with the same referenceId as a deleted field will overwrite
     * the deleted field and restore it to INACTIVE status.</p>
     * <p>Deleted fields cannot be modified.</p>
     */
    public MavenAGIHttpResponse<IntelligentFieldResponse> delete(
            String fieldReferenceId, IntelligentFieldDeleteRequest request, RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl())
                .newBuilder()
                .addPathSegments("v1/intelligent-fields")
                .addPathSegment(fieldReferenceId);
        if (request.getAppId().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "appId", request.getAppId().get(), false);
        }
        if (request.getVariantReferenceId().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl,
                    "variantReferenceId",
                    request.getVariantReferenceId().get(),
                    false);
        }
        if (request.getVariantAppId().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "variantAppId", request.getVariantAppId().get(), false);
        }
        Request.Builder _requestBuilder = new Request.Builder()
                .url(httpUrl.build())
                .method("DELETE", null)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Accept", "application/json");
        Request okhttpRequest = _requestBuilder.build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        try (Response response = client.newCall(okhttpRequest).execute()) {
            ResponseBody responseBody = response.body();
            if (response.isSuccessful()) {
                return new MavenAGIHttpResponse<>(
                        ObjectMappers.JSON_MAPPER.readValue(responseBody.string(), IntelligentFieldResponse.class),
                        response);
            }
            String responseBodyString = responseBody != null ? responseBody.string() : "{}";
            try {
                switch (response.code()) {
                    case 400:
                        throw new BadRequestError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ErrorMessage.class), response);
                    case 404:
                        throw new NotFoundError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ErrorMessage.class), response);
                    case 413:
                        throw new PayloadTooLargeError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ErrorMessage.class), response);
                    case 429:
                        throw new TooManyRequestsError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ErrorMessage.class), response);
                    case 500:
                        throw new ServerError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ErrorMessage.class), response);
                }
            } catch (JsonProcessingException ignored) {
                // unable to map error response, throwing generic error
            }
            throw new MavenAGIApiException(
                    "Error with status code " + response.code(),
                    response.code(),
                    ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class),
                    response);
        } catch (IOException e) {
            throw new MavenAGIException("Network error executing HTTP request", e);
        }
    }

    /**
     * Search computed values for intelligent fields across entities. Supports filtering by field properties and target entity.
     */
    public MavenAGIHttpResponse<IntelligentFieldValueSearchResponse> searchValues() {
        return searchValues(IntelligentFieldValueSearchRequest.builder().build());
    }

    /**
     * Search computed values for intelligent fields across entities. Supports filtering by field properties and target entity.
     */
    public MavenAGIHttpResponse<IntelligentFieldValueSearchResponse> searchValues(
            IntelligentFieldValueSearchRequest request) {
        return searchValues(request, null);
    }

    /**
     * Search computed values for intelligent fields across entities. Supports filtering by field properties and target entity.
     */
    public MavenAGIHttpResponse<IntelligentFieldValueSearchResponse> searchValues(
            IntelligentFieldValueSearchRequest request, RequestOptions requestOptions) {
        HttpUrl httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl())
                .newBuilder()
                .addPathSegments("v1/intelligent-fields")
                .addPathSegments("values/search")
                .build();
        RequestBody body;
        try {
            body = RequestBody.create(
                    ObjectMappers.JSON_MAPPER.writeValueAsBytes(request), MediaTypes.APPLICATION_JSON);
        } catch (JsonProcessingException e) {
            throw new MavenAGIException("Failed to serialize request", e);
        }
        Request okhttpRequest = new Request.Builder()
                .url(httpUrl)
                .method("POST", body)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Content-Type", "application/json")
                .addHeader("Accept", "application/json")
                .build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        try (Response response = client.newCall(okhttpRequest).execute()) {
            ResponseBody responseBody = response.body();
            if (response.isSuccessful()) {
                return new MavenAGIHttpResponse<>(
                        ObjectMappers.JSON_MAPPER.readValue(
                                responseBody.string(), IntelligentFieldValueSearchResponse.class),
                        response);
            }
            String responseBodyString = responseBody != null ? responseBody.string() : "{}";
            try {
                switch (response.code()) {
                    case 400:
                        throw new BadRequestError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ErrorMessage.class), response);
                    case 404:
                        throw new NotFoundError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ErrorMessage.class), response);
                    case 413:
                        throw new PayloadTooLargeError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ErrorMessage.class), response);
                    case 429:
                        throw new TooManyRequestsError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ErrorMessage.class), response);
                    case 500:
                        throw new ServerError(
                                ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ErrorMessage.class), response);
                }
            } catch (JsonProcessingException ignored) {
                // unable to map error response, throwing generic error
            }
            throw new MavenAGIApiException(
                    "Error with status code " + response.code(),
                    response.code(),
                    ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Object.class),
                    response);
        } catch (IOException e) {
            throw new MavenAGIException("Network error executing HTTP request", e);
        }
    }
}
