/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.mavenagi.api.resources.commons.types;

import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonIgnoreProperties;
import com.fasterxml.jackson.annotation.JsonProperty;
import com.fasterxml.jackson.annotation.JsonSubTypes;
import com.fasterxml.jackson.annotation.JsonTypeInfo;
import com.fasterxml.jackson.annotation.JsonTypeName;
import com.fasterxml.jackson.annotation.JsonUnwrapped;
import com.fasterxml.jackson.annotation.JsonValue;
import java.util.Objects;
import java.util.Optional;

public final class ConversationPrecondition {
    private final Value value;

    @JsonCreator(mode = JsonCreator.Mode.DELEGATING)
    private ConversationPrecondition(Value value) {
        this.value = value;
    }

    public <T> T visit(Visitor<T> visitor) {
        return value.visit(visitor);
    }

    public static ConversationPrecondition tags(TagsPrecondition value) {
        return new ConversationPrecondition(new TagsValue(value));
    }

    public static ConversationPrecondition metadata(MetadataPrecondition value) {
        return new ConversationPrecondition(new MetadataValue(value));
    }

    public static ConversationPrecondition actionExecuted(ConversationExecutedActionPrecondition value) {
        return new ConversationPrecondition(new ActionExecutedValue(value));
    }

    public boolean isTags() {
        return value instanceof TagsValue;
    }

    public boolean isMetadata() {
        return value instanceof MetadataValue;
    }

    public boolean isActionExecuted() {
        return value instanceof ActionExecutedValue;
    }

    public boolean _isUnknown() {
        return value instanceof _UnknownValue;
    }

    public Optional<TagsPrecondition> getTags() {
        if (isTags()) {
            return Optional.of(((TagsValue) value).value);
        }
        return Optional.empty();
    }

    public Optional<MetadataPrecondition> getMetadata() {
        if (isMetadata()) {
            return Optional.of(((MetadataValue) value).value);
        }
        return Optional.empty();
    }

    public Optional<ConversationExecutedActionPrecondition> getActionExecuted() {
        if (isActionExecuted()) {
            return Optional.of(((ActionExecutedValue) value).value);
        }
        return Optional.empty();
    }

    public Optional<Object> _getUnknown() {
        if (_isUnknown()) {
            return Optional.of(((_UnknownValue) value).value);
        }
        return Optional.empty();
    }

    @JsonValue
    private Value getValue() {
        return this.value;
    }

    public interface Visitor<T> {
        T visitTags(TagsPrecondition tags);

        T visitMetadata(MetadataPrecondition metadata);

        T visitActionExecuted(ConversationExecutedActionPrecondition actionExecuted);

        T _visitUnknown(Object unknownType);
    }

    @JsonTypeInfo(
            use = JsonTypeInfo.Id.NAME,
            property = "conversationPreconditionType",
            visible = true,
            defaultImpl = _UnknownValue.class)
    @JsonSubTypes({
        @JsonSubTypes.Type(TagsValue.class),
        @JsonSubTypes.Type(MetadataValue.class),
        @JsonSubTypes.Type(ActionExecutedValue.class)
    })
    @JsonIgnoreProperties(ignoreUnknown = true)
    private interface Value {
        <T> T visit(Visitor<T> visitor);
    }

    @JsonTypeName("tags")
    private static final class TagsValue implements Value {
        @JsonUnwrapped
        private TagsPrecondition value;

        @JsonCreator(mode = JsonCreator.Mode.PROPERTIES)
        private TagsValue() {}

        private TagsValue(TagsPrecondition value) {
            this.value = value;
        }

        @java.lang.Override
        public <T> T visit(Visitor<T> visitor) {
            return visitor.visitTags(value);
        }

        @java.lang.Override
        public boolean equals(Object other) {
            if (this == other) return true;
            return other instanceof TagsValue && equalTo((TagsValue) other);
        }

        private boolean equalTo(TagsValue other) {
            return value.equals(other.value);
        }

        @java.lang.Override
        public int hashCode() {
            return Objects.hash(this.value);
        }

        @java.lang.Override
        public String toString() {
            return "ConversationPrecondition{" + "value: " + value + "}";
        }
    }

    @JsonTypeName("metadata")
    private static final class MetadataValue implements Value {
        @JsonUnwrapped
        private MetadataPrecondition value;

        @JsonCreator(mode = JsonCreator.Mode.PROPERTIES)
        private MetadataValue() {}

        private MetadataValue(MetadataPrecondition value) {
            this.value = value;
        }

        @java.lang.Override
        public <T> T visit(Visitor<T> visitor) {
            return visitor.visitMetadata(value);
        }

        @java.lang.Override
        public boolean equals(Object other) {
            if (this == other) return true;
            return other instanceof MetadataValue && equalTo((MetadataValue) other);
        }

        private boolean equalTo(MetadataValue other) {
            return value.equals(other.value);
        }

        @java.lang.Override
        public int hashCode() {
            return Objects.hash(this.value);
        }

        @java.lang.Override
        public String toString() {
            return "ConversationPrecondition{" + "value: " + value + "}";
        }
    }

    @JsonTypeName("actionExecuted")
    private static final class ActionExecutedValue implements Value {
        @JsonUnwrapped
        private ConversationExecutedActionPrecondition value;

        @JsonCreator(mode = JsonCreator.Mode.PROPERTIES)
        private ActionExecutedValue() {}

        private ActionExecutedValue(ConversationExecutedActionPrecondition value) {
            this.value = value;
        }

        @java.lang.Override
        public <T> T visit(Visitor<T> visitor) {
            return visitor.visitActionExecuted(value);
        }

        @java.lang.Override
        public boolean equals(Object other) {
            if (this == other) return true;
            return other instanceof ActionExecutedValue && equalTo((ActionExecutedValue) other);
        }

        private boolean equalTo(ActionExecutedValue other) {
            return value.equals(other.value);
        }

        @java.lang.Override
        public int hashCode() {
            return Objects.hash(this.value);
        }

        @java.lang.Override
        public String toString() {
            return "ConversationPrecondition{" + "value: " + value + "}";
        }
    }

    private static final class _UnknownValue implements Value {
        private String type;

        @JsonValue
        private Object value;

        @JsonCreator(mode = JsonCreator.Mode.PROPERTIES)
        private _UnknownValue(@JsonProperty("value") Object value) {}

        @java.lang.Override
        public <T> T visit(Visitor<T> visitor) {
            return visitor._visitUnknown(value);
        }

        @java.lang.Override
        public boolean equals(Object other) {
            if (this == other) return true;
            return other instanceof _UnknownValue && equalTo((_UnknownValue) other);
        }

        private boolean equalTo(_UnknownValue other) {
            return type.equals(other.type) && value.equals(other.value);
        }

        @java.lang.Override
        public int hashCode() {
            return Objects.hash(this.type, this.value);
        }

        @java.lang.Override
        public String toString() {
            return "ConversationPrecondition{" + "type: " + type + ", value: " + value + "}";
        }
    }
}
