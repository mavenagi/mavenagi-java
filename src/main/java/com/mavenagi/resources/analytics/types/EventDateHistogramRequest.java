/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.mavenagi.resources.analytics.types;

import com.fasterxml.jackson.annotation.JsonAnyGetter;
import com.fasterxml.jackson.annotation.JsonAnySetter;
import com.fasterxml.jackson.annotation.JsonIgnoreProperties;
import com.fasterxml.jackson.annotation.JsonInclude;
import com.fasterxml.jackson.annotation.JsonProperty;
import com.fasterxml.jackson.annotation.JsonSetter;
import com.fasterxml.jackson.annotation.Nulls;
import com.fasterxml.jackson.databind.annotation.JsonDeserialize;
import com.mavenagi.core.ObjectMappers;
import com.mavenagi.resources.commons.types.EventFilter;
import java.util.HashMap;
import java.util.Map;
import java.util.Objects;
import java.util.Optional;
import org.jetbrains.annotations.NotNull;

@JsonInclude(JsonInclude.Include.NON_ABSENT)
@JsonDeserialize(builder = EventDateHistogramRequest.Builder.class)
public final class EventDateHistogramRequest implements IEventAnalyticsRequest {
    private final Optional<EventFilter> eventFilter;

    private final Optional<String> timezone;

    private final TimeInterval timeInterval;

    private final Optional<EventGroupBy> groupBy;

    private final EventMetric metric;

    private final Map<String, Object> additionalProperties;

    private EventDateHistogramRequest(
            Optional<EventFilter> eventFilter,
            Optional<String> timezone,
            TimeInterval timeInterval,
            Optional<EventGroupBy> groupBy,
            EventMetric metric,
            Map<String, Object> additionalProperties) {
        this.eventFilter = eventFilter;
        this.timezone = timezone;
        this.timeInterval = timeInterval;
        this.groupBy = groupBy;
        this.metric = metric;
        this.additionalProperties = additionalProperties;
    }

    /**
     * @return Optional filter applied to refine the event data before processing.
     */
    @JsonProperty("eventFilter")
    @java.lang.Override
    public Optional<EventFilter> getEventFilter() {
        return eventFilter;
    }

    /**
     * @return IANA timezone identifier (e.g., &quot;America/Los_Angeles&quot;).
     * When provided, time-based groupings (e.g., DAY) and date filters are evaluated in this timezone;
     * otherwise UTC is used.
     */
    @JsonProperty("timezone")
    @java.lang.Override
    public Optional<String> getTimezone() {
        return timezone;
    }

    /**
     * @return Time-based grouping interval (e.g., HOUR, DAY, WEEK) for the date histogram.
     */
    @JsonProperty("timeInterval")
    public TimeInterval getTimeInterval() {
        return timeInterval;
    }

    /**
     * @return Groups data before applying calculations, forming a separate time series for each group.
     */
    @JsonProperty("groupBy")
    public Optional<EventGroupBy> getGroupBy() {
        return groupBy;
    }

    /**
     * @return Defines the y-axis values for the date histogram.
     */
    @JsonProperty("metric")
    public EventMetric getMetric() {
        return metric;
    }

    @java.lang.Override
    public boolean equals(Object other) {
        if (this == other) return true;
        return other instanceof EventDateHistogramRequest && equalTo((EventDateHistogramRequest) other);
    }

    @JsonAnyGetter
    public Map<String, Object> getAdditionalProperties() {
        return this.additionalProperties;
    }

    private boolean equalTo(EventDateHistogramRequest other) {
        return eventFilter.equals(other.eventFilter)
                && timezone.equals(other.timezone)
                && timeInterval.equals(other.timeInterval)
                && groupBy.equals(other.groupBy)
                && metric.equals(other.metric);
    }

    @java.lang.Override
    public int hashCode() {
        return Objects.hash(this.eventFilter, this.timezone, this.timeInterval, this.groupBy, this.metric);
    }

    @java.lang.Override
    public String toString() {
        return ObjectMappers.stringify(this);
    }

    public static TimeIntervalStage builder() {
        return new Builder();
    }

    public interface TimeIntervalStage {
        /**
         * <p>Time-based grouping interval (e.g., HOUR, DAY, WEEK) for the date histogram.</p>
         */
        MetricStage timeInterval(@NotNull TimeInterval timeInterval);

        Builder from(EventDateHistogramRequest other);
    }

    public interface MetricStage {
        /**
         * <p>Defines the y-axis values for the date histogram.</p>
         */
        _FinalStage metric(@NotNull EventMetric metric);
    }

    public interface _FinalStage {
        EventDateHistogramRequest build();

        /**
         * <p>Optional filter applied to refine the event data before processing.</p>
         */
        _FinalStage eventFilter(Optional<EventFilter> eventFilter);

        _FinalStage eventFilter(EventFilter eventFilter);

        /**
         * <p>IANA timezone identifier (e.g., &quot;America/Los_Angeles&quot;).
         * When provided, time-based groupings (e.g., DAY) and date filters are evaluated in this timezone;
         * otherwise UTC is used.</p>
         */
        _FinalStage timezone(Optional<String> timezone);

        _FinalStage timezone(String timezone);

        /**
         * <p>Groups data before applying calculations, forming a separate time series for each group.</p>
         */
        _FinalStage groupBy(Optional<EventGroupBy> groupBy);

        _FinalStage groupBy(EventGroupBy groupBy);
    }

    @JsonIgnoreProperties(ignoreUnknown = true)
    public static final class Builder implements TimeIntervalStage, MetricStage, _FinalStage {
        private TimeInterval timeInterval;

        private EventMetric metric;

        private Optional<EventGroupBy> groupBy = Optional.empty();

        private Optional<String> timezone = Optional.empty();

        private Optional<EventFilter> eventFilter = Optional.empty();

        @JsonAnySetter
        private Map<String, Object> additionalProperties = new HashMap<>();

        private Builder() {}

        @java.lang.Override
        public Builder from(EventDateHistogramRequest other) {
            eventFilter(other.getEventFilter());
            timezone(other.getTimezone());
            timeInterval(other.getTimeInterval());
            groupBy(other.getGroupBy());
            metric(other.getMetric());
            return this;
        }

        /**
         * <p>Time-based grouping interval (e.g., HOUR, DAY, WEEK) for the date histogram.</p>
         * <p>Time-based grouping interval (e.g., HOUR, DAY, WEEK) for the date histogram.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("timeInterval")
        public MetricStage timeInterval(@NotNull TimeInterval timeInterval) {
            this.timeInterval = Objects.requireNonNull(timeInterval, "timeInterval must not be null");
            return this;
        }

        /**
         * <p>Defines the y-axis values for the date histogram.</p>
         * <p>Defines the y-axis values for the date histogram.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("metric")
        public _FinalStage metric(@NotNull EventMetric metric) {
            this.metric = Objects.requireNonNull(metric, "metric must not be null");
            return this;
        }

        /**
         * <p>Groups data before applying calculations, forming a separate time series for each group.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage groupBy(EventGroupBy groupBy) {
            this.groupBy = Optional.ofNullable(groupBy);
            return this;
        }

        /**
         * <p>Groups data before applying calculations, forming a separate time series for each group.</p>
         */
        @java.lang.Override
        @JsonSetter(value = "groupBy", nulls = Nulls.SKIP)
        public _FinalStage groupBy(Optional<EventGroupBy> groupBy) {
            this.groupBy = groupBy;
            return this;
        }

        /**
         * <p>IANA timezone identifier (e.g., &quot;America/Los_Angeles&quot;).
         * When provided, time-based groupings (e.g., DAY) and date filters are evaluated in this timezone;
         * otherwise UTC is used.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage timezone(String timezone) {
            this.timezone = Optional.ofNullable(timezone);
            return this;
        }

        /**
         * <p>IANA timezone identifier (e.g., &quot;America/Los_Angeles&quot;).
         * When provided, time-based groupings (e.g., DAY) and date filters are evaluated in this timezone;
         * otherwise UTC is used.</p>
         */
        @java.lang.Override
        @JsonSetter(value = "timezone", nulls = Nulls.SKIP)
        public _FinalStage timezone(Optional<String> timezone) {
            this.timezone = timezone;
            return this;
        }

        /**
         * <p>Optional filter applied to refine the event data before processing.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage eventFilter(EventFilter eventFilter) {
            this.eventFilter = Optional.ofNullable(eventFilter);
            return this;
        }

        /**
         * <p>Optional filter applied to refine the event data before processing.</p>
         */
        @java.lang.Override
        @JsonSetter(value = "eventFilter", nulls = Nulls.SKIP)
        public _FinalStage eventFilter(Optional<EventFilter> eventFilter) {
            this.eventFilter = eventFilter;
            return this;
        }

        @java.lang.Override
        public EventDateHistogramRequest build() {
            return new EventDateHistogramRequest(
                    eventFilter, timezone, timeInterval, groupBy, metric, additionalProperties);
        }
    }
}
