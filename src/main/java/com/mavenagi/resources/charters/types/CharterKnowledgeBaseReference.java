/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.mavenagi.resources.charters.types;

import com.fasterxml.jackson.annotation.JsonAnyGetter;
import com.fasterxml.jackson.annotation.JsonAnySetter;
import com.fasterxml.jackson.annotation.JsonIgnoreProperties;
import com.fasterxml.jackson.annotation.JsonInclude;
import com.fasterxml.jackson.annotation.JsonProperty;
import com.fasterxml.jackson.annotation.JsonSetter;
import com.fasterxml.jackson.annotation.Nulls;
import com.fasterxml.jackson.databind.annotation.JsonDeserialize;
import com.mavenagi.core.ObjectMappers;
import com.mavenagi.resources.commons.types.EntityId;
import com.mavenagi.resources.commons.types.MetadataFilter;
import java.util.HashMap;
import java.util.Map;
import java.util.Objects;
import java.util.Optional;
import org.jetbrains.annotations.NotNull;

@JsonInclude(JsonInclude.Include.NON_ABSENT)
@JsonDeserialize(builder = CharterKnowledgeBaseReference.Builder.class)
public final class CharterKnowledgeBaseReference {
    private final EntityId knowledgeBaseId;

    private final Optional<MetadataFilter> documentFilter;

    private final Map<String, Object> additionalProperties;

    private CharterKnowledgeBaseReference(
            EntityId knowledgeBaseId,
            Optional<MetadataFilter> documentFilter,
            Map<String, Object> additionalProperties) {
        this.knowledgeBaseId = knowledgeBaseId;
        this.documentFilter = documentFilter;
        this.additionalProperties = additionalProperties;
    }

    /**
     * @return The knowledge base this charter makes available.
     */
    @JsonProperty("knowledgeBaseId")
    public EntityId getKnowledgeBaseId() {
        return knowledgeBaseId;
    }

    /**
     * @return Narrows this reference to the documents in the knowledge base whose <code>metadata</code>
     * satisfies this filter. Omit to reference every document in the knowledge base.
     * <p>An entry's <code>key</code> is a metadata key on the document. A document carrying no such
     * key matches only <code>presence</code> <code>IS_UNDETERMINED</code>.</p>
     */
    @JsonProperty("documentFilter")
    public Optional<MetadataFilter> getDocumentFilter() {
        return documentFilter;
    }

    @java.lang.Override
    public boolean equals(Object other) {
        if (this == other) return true;
        return other instanceof CharterKnowledgeBaseReference && equalTo((CharterKnowledgeBaseReference) other);
    }

    @JsonAnyGetter
    public Map<String, Object> getAdditionalProperties() {
        return this.additionalProperties;
    }

    private boolean equalTo(CharterKnowledgeBaseReference other) {
        return knowledgeBaseId.equals(other.knowledgeBaseId) && documentFilter.equals(other.documentFilter);
    }

    @java.lang.Override
    public int hashCode() {
        return Objects.hash(this.knowledgeBaseId, this.documentFilter);
    }

    @java.lang.Override
    public String toString() {
        return ObjectMappers.stringify(this);
    }

    public static KnowledgeBaseIdStage builder() {
        return new Builder();
    }

    public interface KnowledgeBaseIdStage {
        /**
         * <p>The knowledge base this charter makes available.</p>
         */
        _FinalStage knowledgeBaseId(@NotNull EntityId knowledgeBaseId);

        Builder from(CharterKnowledgeBaseReference other);
    }

    public interface _FinalStage {
        CharterKnowledgeBaseReference build();

        /**
         * <p>Narrows this reference to the documents in the knowledge base whose <code>metadata</code>
         * satisfies this filter. Omit to reference every document in the knowledge base.</p>
         * <p>An entry's <code>key</code> is a metadata key on the document. A document carrying no such
         * key matches only <code>presence</code> <code>IS_UNDETERMINED</code>.</p>
         */
        _FinalStage documentFilter(Optional<MetadataFilter> documentFilter);

        _FinalStage documentFilter(MetadataFilter documentFilter);
    }

    @JsonIgnoreProperties(ignoreUnknown = true)
    public static final class Builder implements KnowledgeBaseIdStage, _FinalStage {
        private EntityId knowledgeBaseId;

        private Optional<MetadataFilter> documentFilter = Optional.empty();

        @JsonAnySetter
        private Map<String, Object> additionalProperties = new HashMap<>();

        private Builder() {}

        @java.lang.Override
        public Builder from(CharterKnowledgeBaseReference other) {
            knowledgeBaseId(other.getKnowledgeBaseId());
            documentFilter(other.getDocumentFilter());
            return this;
        }

        /**
         * <p>The knowledge base this charter makes available.</p>
         * <p>The knowledge base this charter makes available.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("knowledgeBaseId")
        public _FinalStage knowledgeBaseId(@NotNull EntityId knowledgeBaseId) {
            this.knowledgeBaseId = Objects.requireNonNull(knowledgeBaseId, "knowledgeBaseId must not be null");
            return this;
        }

        /**
         * <p>Narrows this reference to the documents in the knowledge base whose <code>metadata</code>
         * satisfies this filter. Omit to reference every document in the knowledge base.</p>
         * <p>An entry's <code>key</code> is a metadata key on the document. A document carrying no such
         * key matches only <code>presence</code> <code>IS_UNDETERMINED</code>.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage documentFilter(MetadataFilter documentFilter) {
            this.documentFilter = Optional.ofNullable(documentFilter);
            return this;
        }

        /**
         * <p>Narrows this reference to the documents in the knowledge base whose <code>metadata</code>
         * satisfies this filter. Omit to reference every document in the knowledge base.</p>
         * <p>An entry's <code>key</code> is a metadata key on the document. A document carrying no such
         * key matches only <code>presence</code> <code>IS_UNDETERMINED</code>.</p>
         */
        @java.lang.Override
        @JsonSetter(value = "documentFilter", nulls = Nulls.SKIP)
        public _FinalStage documentFilter(Optional<MetadataFilter> documentFilter) {
            this.documentFilter = documentFilter;
            return this;
        }

        @java.lang.Override
        public CharterKnowledgeBaseReference build() {
            return new CharterKnowledgeBaseReference(knowledgeBaseId, documentFilter, additionalProperties);
        }
    }
}
