/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.mavenagi.resources.segments.types;

import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonValue;

public final class SegmentStatus {
    public static final SegmentStatus DELETED = new SegmentStatus(Value.DELETED, "DELETED");

    public static final SegmentStatus ACTIVE = new SegmentStatus(Value.ACTIVE, "ACTIVE");

    public static final SegmentStatus INACTIVE = new SegmentStatus(Value.INACTIVE, "INACTIVE");

    private final Value value;

    private final String string;

    SegmentStatus(Value value, String string) {
        this.value = value;
        this.string = string;
    }

    public Value getEnumValue() {
        return value;
    }

    @java.lang.Override
    @JsonValue
    public String toString() {
        return this.string;
    }

    @java.lang.Override
    public boolean equals(Object other) {
        return (this == other)
                || (other instanceof SegmentStatus && this.string.equals(((SegmentStatus) other).string));
    }

    @java.lang.Override
    public int hashCode() {
        return this.string.hashCode();
    }

    public <T> T visit(Visitor<T> visitor) {
        switch (value) {
            case DELETED:
                return visitor.visitDeleted();
            case ACTIVE:
                return visitor.visitActive();
            case INACTIVE:
                return visitor.visitInactive();
            case UNKNOWN:
            default:
                return visitor.visitUnknown(string);
        }
    }

    @JsonCreator(mode = JsonCreator.Mode.DELEGATING)
    public static SegmentStatus valueOf(String value) {
        switch (value) {
            case "DELETED":
                return DELETED;
            case "ACTIVE":
                return ACTIVE;
            case "INACTIVE":
                return INACTIVE;
            default:
                return new SegmentStatus(Value.UNKNOWN, value);
        }
    }

    public enum Value {
        ACTIVE,

        INACTIVE,

        DELETED,

        UNKNOWN
    }

    public interface Visitor<T> {
        T visitActive();

        T visitInactive();

        T visitDeleted();

        T visitUnknown(String unknownType);
    }
}
