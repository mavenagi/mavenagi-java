/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.mavenagi.resources.commons.types;

import com.fasterxml.jackson.annotation.JsonAnyGetter;
import com.fasterxml.jackson.annotation.JsonAnySetter;
import com.fasterxml.jackson.annotation.JsonIgnoreProperties;
import com.fasterxml.jackson.annotation.JsonInclude;
import com.fasterxml.jackson.annotation.JsonProperty;
import com.fasterxml.jackson.annotation.JsonSetter;
import com.fasterxml.jackson.databind.annotation.JsonDeserialize;
import com.mavenagi.core.ObjectMappers;
import java.util.HashMap;
import java.util.Map;
import java.util.Objects;
import org.jetbrains.annotations.NotNull;

@JsonInclude(JsonInclude.Include.NON_ABSENT)
@JsonDeserialize(builder = KeyValueEntryCondition.Builder.class)
public final class KeyValueEntryCondition {
    private final String path;

    private final FieldCondition condition;

    private final Map<String, Object> additionalProperties;

    private KeyValueEntryCondition(String path, FieldCondition condition, Map<String, Object> additionalProperties) {
        this.path = path;
        this.condition = condition;
        this.additionalProperties = additionalProperties;
    }

    /**
     * @return Dot-separated path to a value inside the object, e.g. <code>name</code> or <code>customer.tier</code>. Array elements are indexed with brackets (<code>orders[0].id</code>), and a key containing a dot needs bracket notation (<code>['a.b']</code>). A path that reaches nothing resolves to undetermined rather than failing.
     * The path must address a single value. Wildcards, filters, slices and recursive descent (<code>orders[*].total</code>, <code>$..total</code>) address many values and are rejected, as is a path with a syntax error.
     */
    @JsonProperty("path")
    public String getPath() {
        return path;
    }

    /**
     * @return The condition to evaluate against the value at <code>path</code>.
     */
    @JsonProperty("condition")
    public FieldCondition getCondition() {
        return condition;
    }

    @java.lang.Override
    public boolean equals(Object other) {
        if (this == other) return true;
        return other instanceof KeyValueEntryCondition && equalTo((KeyValueEntryCondition) other);
    }

    @JsonAnyGetter
    public Map<String, Object> getAdditionalProperties() {
        return this.additionalProperties;
    }

    private boolean equalTo(KeyValueEntryCondition other) {
        return path.equals(other.path) && condition.equals(other.condition);
    }

    @java.lang.Override
    public int hashCode() {
        return Objects.hash(this.path, this.condition);
    }

    @java.lang.Override
    public String toString() {
        return ObjectMappers.stringify(this);
    }

    public static PathStage builder() {
        return new Builder();
    }

    public interface PathStage {
        /**
         * <p>Dot-separated path to a value inside the object, e.g. <code>name</code> or <code>customer.tier</code>. Array elements are indexed with brackets (<code>orders[0].id</code>), and a key containing a dot needs bracket notation (<code>['a.b']</code>). A path that reaches nothing resolves to undetermined rather than failing.
         * The path must address a single value. Wildcards, filters, slices and recursive descent (<code>orders[*].total</code>, <code>$..total</code>) address many values and are rejected, as is a path with a syntax error.</p>
         */
        ConditionStage path(@NotNull String path);

        Builder from(KeyValueEntryCondition other);
    }

    public interface ConditionStage {
        /**
         * <p>The condition to evaluate against the value at <code>path</code>.</p>
         */
        _FinalStage condition(@NotNull FieldCondition condition);
    }

    public interface _FinalStage {
        KeyValueEntryCondition build();
    }

    @JsonIgnoreProperties(ignoreUnknown = true)
    public static final class Builder implements PathStage, ConditionStage, _FinalStage {
        private String path;

        private FieldCondition condition;

        @JsonAnySetter
        private Map<String, Object> additionalProperties = new HashMap<>();

        private Builder() {}

        @java.lang.Override
        public Builder from(KeyValueEntryCondition other) {
            path(other.getPath());
            condition(other.getCondition());
            return this;
        }

        /**
         * <p>Dot-separated path to a value inside the object, e.g. <code>name</code> or <code>customer.tier</code>. Array elements are indexed with brackets (<code>orders[0].id</code>), and a key containing a dot needs bracket notation (<code>['a.b']</code>). A path that reaches nothing resolves to undetermined rather than failing.
         * The path must address a single value. Wildcards, filters, slices and recursive descent (<code>orders[*].total</code>, <code>$..total</code>) address many values and are rejected, as is a path with a syntax error.</p>
         * <p>Dot-separated path to a value inside the object, e.g. <code>name</code> or <code>customer.tier</code>. Array elements are indexed with brackets (<code>orders[0].id</code>), and a key containing a dot needs bracket notation (<code>['a.b']</code>). A path that reaches nothing resolves to undetermined rather than failing.
         * The path must address a single value. Wildcards, filters, slices and recursive descent (<code>orders[*].total</code>, <code>$..total</code>) address many values and are rejected, as is a path with a syntax error.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("path")
        public ConditionStage path(@NotNull String path) {
            this.path = Objects.requireNonNull(path, "path must not be null");
            return this;
        }

        /**
         * <p>The condition to evaluate against the value at <code>path</code>.</p>
         * <p>The condition to evaluate against the value at <code>path</code>.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("condition")
        public _FinalStage condition(@NotNull FieldCondition condition) {
            this.condition = Objects.requireNonNull(condition, "condition must not be null");
            return this;
        }

        @java.lang.Override
        public KeyValueEntryCondition build() {
            return new KeyValueEntryCondition(path, condition, additionalProperties);
        }
    }
}
