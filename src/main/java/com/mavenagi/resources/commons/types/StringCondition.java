/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.mavenagi.resources.commons.types;

import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonIgnoreProperties;
import com.fasterxml.jackson.annotation.JsonProperty;
import com.fasterxml.jackson.annotation.JsonSubTypes;
import com.fasterxml.jackson.annotation.JsonTypeInfo;
import com.fasterxml.jackson.annotation.JsonTypeName;
import com.fasterxml.jackson.annotation.JsonUnwrapped;
import com.fasterxml.jackson.annotation.JsonValue;
import java.util.Objects;
import java.util.Optional;

public final class StringCondition {
    private final Value value;

    @JsonCreator(mode = JsonCreator.Mode.DELEGATING)
    private StringCondition(Value value) {
        this.value = value;
    }

    public <T> T visit(Visitor<T> visitor) {
        return value.visit(visitor);
    }

    public static StringCondition comparison(StringComparisonCondition value) {
        return new StringCondition(new ComparisonValue(value));
    }

    public static StringCondition membership(StringMembershipCondition value) {
        return new StringCondition(new MembershipValue(value));
    }

    public boolean isComparison() {
        return value instanceof ComparisonValue;
    }

    public boolean isMembership() {
        return value instanceof MembershipValue;
    }

    public boolean _isUnknown() {
        return value instanceof _UnknownValue;
    }

    public Optional<StringComparisonCondition> getComparison() {
        if (isComparison()) {
            return Optional.of(((ComparisonValue) value).value);
        }
        return Optional.empty();
    }

    public Optional<StringMembershipCondition> getMembership() {
        if (isMembership()) {
            return Optional.of(((MembershipValue) value).value);
        }
        return Optional.empty();
    }

    public Optional<Object> _getUnknown() {
        if (_isUnknown()) {
            return Optional.of(((_UnknownValue) value).value);
        }
        return Optional.empty();
    }

    @JsonValue
    private Value getValue() {
        return this.value;
    }

    public interface Visitor<T> {
        T visitComparison(StringComparisonCondition comparison);

        T visitMembership(StringMembershipCondition membership);

        T _visitUnknown(Object unknownType);
    }

    @JsonTypeInfo(use = JsonTypeInfo.Id.NAME, property = "opType", visible = true, defaultImpl = _UnknownValue.class)
    @JsonSubTypes({@JsonSubTypes.Type(ComparisonValue.class), @JsonSubTypes.Type(MembershipValue.class)})
    @JsonIgnoreProperties(ignoreUnknown = true)
    private interface Value {
        <T> T visit(Visitor<T> visitor);
    }

    @JsonTypeName("comparison")
    @JsonIgnoreProperties("opType")
    private static final class ComparisonValue implements Value {
        @JsonUnwrapped
        private StringComparisonCondition value;

        @JsonCreator(mode = JsonCreator.Mode.PROPERTIES)
        private ComparisonValue() {}

        private ComparisonValue(StringComparisonCondition value) {
            this.value = value;
        }

        @java.lang.Override
        public <T> T visit(Visitor<T> visitor) {
            return visitor.visitComparison(value);
        }

        @java.lang.Override
        public boolean equals(Object other) {
            if (this == other) return true;
            return other instanceof ComparisonValue && equalTo((ComparisonValue) other);
        }

        private boolean equalTo(ComparisonValue other) {
            return value.equals(other.value);
        }

        @java.lang.Override
        public int hashCode() {
            return Objects.hash(this.value);
        }

        @java.lang.Override
        public String toString() {
            return "StringCondition{" + "value: " + value + "}";
        }
    }

    @JsonTypeName("membership")
    @JsonIgnoreProperties("opType")
    private static final class MembershipValue implements Value {
        @JsonUnwrapped
        private StringMembershipCondition value;

        @JsonCreator(mode = JsonCreator.Mode.PROPERTIES)
        private MembershipValue() {}

        private MembershipValue(StringMembershipCondition value) {
            this.value = value;
        }

        @java.lang.Override
        public <T> T visit(Visitor<T> visitor) {
            return visitor.visitMembership(value);
        }

        @java.lang.Override
        public boolean equals(Object other) {
            if (this == other) return true;
            return other instanceof MembershipValue && equalTo((MembershipValue) other);
        }

        private boolean equalTo(MembershipValue other) {
            return value.equals(other.value);
        }

        @java.lang.Override
        public int hashCode() {
            return Objects.hash(this.value);
        }

        @java.lang.Override
        public String toString() {
            return "StringCondition{" + "value: " + value + "}";
        }
    }

    @JsonIgnoreProperties("opType")
    private static final class _UnknownValue implements Value {
        private String type;

        @JsonValue
        private Object value;

        @JsonCreator(mode = JsonCreator.Mode.PROPERTIES)
        private _UnknownValue(@JsonProperty("value") Object value) {}

        @java.lang.Override
        public <T> T visit(Visitor<T> visitor) {
            return visitor._visitUnknown(value);
        }

        @java.lang.Override
        public boolean equals(Object other) {
            if (this == other) return true;
            return other instanceof _UnknownValue && equalTo((_UnknownValue) other);
        }

        private boolean equalTo(_UnknownValue other) {
            return type.equals(other.type) && value.equals(other.value);
        }

        @java.lang.Override
        public int hashCode() {
            return Objects.hash(this.type, this.value);
        }

        @java.lang.Override
        public String toString() {
            return "StringCondition{" + "type: " + type + ", value: " + value + "}";
        }
    }
}
