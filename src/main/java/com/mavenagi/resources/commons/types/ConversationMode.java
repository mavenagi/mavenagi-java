/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.mavenagi.resources.commons.types;

import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonValue;

public final class ConversationMode {
    public static final ConversationMode VOICE = new ConversationMode(Value.VOICE, "VOICE");

    public static final ConversationMode TEXT = new ConversationMode(Value.TEXT, "TEXT");

    private final Value value;

    private final String string;

    ConversationMode(Value value, String string) {
        this.value = value;
        this.string = string;
    }

    public Value getEnumValue() {
        return value;
    }

    @java.lang.Override
    @JsonValue
    public String toString() {
        return this.string;
    }

    @java.lang.Override
    public boolean equals(Object other) {
        return (this == other)
                || (other instanceof ConversationMode && this.string.equals(((ConversationMode) other).string));
    }

    @java.lang.Override
    public int hashCode() {
        return this.string.hashCode();
    }

    public <T> T visit(Visitor<T> visitor) {
        switch (value) {
            case VOICE:
                return visitor.visitVoice();
            case TEXT:
                return visitor.visitText();
            case UNKNOWN:
            default:
                return visitor.visitUnknown(string);
        }
    }

    @JsonCreator(mode = JsonCreator.Mode.DELEGATING)
    public static ConversationMode valueOf(String value) {
        switch (value) {
            case "VOICE":
                return VOICE;
            case "TEXT":
                return TEXT;
            default:
                return new ConversationMode(Value.UNKNOWN, value);
        }
    }

    public enum Value {
        VOICE,

        TEXT,

        UNKNOWN
    }

    public interface Visitor<T> {
        T visitVoice();

        T visitText();

        T visitUnknown(String unknownType);
    }
}
