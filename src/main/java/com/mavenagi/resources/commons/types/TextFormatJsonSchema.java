/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.mavenagi.resources.commons.types;

import com.fasterxml.jackson.annotation.JsonAnyGetter;
import com.fasterxml.jackson.annotation.JsonAnySetter;
import com.fasterxml.jackson.annotation.JsonIgnoreProperties;
import com.fasterxml.jackson.annotation.JsonInclude;
import com.fasterxml.jackson.annotation.JsonProperty;
import com.fasterxml.jackson.annotation.JsonSetter;
import com.fasterxml.jackson.annotation.Nulls;
import com.fasterxml.jackson.databind.annotation.JsonDeserialize;
import com.mavenagi.core.ObjectMappers;
import java.util.HashMap;
import java.util.Map;
import java.util.Objects;
import java.util.Optional;

@JsonInclude(JsonInclude.Include.NON_ABSENT)
@JsonDeserialize(builder = TextFormatJsonSchema.Builder.class)
public final class TextFormatJsonSchema {
    private final Object schema;

    private final Optional<String> description;

    private final Map<String, Object> additionalProperties;

    private TextFormatJsonSchema(
            Object schema, Optional<String> description, Map<String, Object> additionalProperties) {
        this.schema = schema;
        this.description = description;
        this.additionalProperties = additionalProperties;
    }

    /**
     * @return The required shape. Property names and their <code>description</code>s are what tell the agent how
     * to fill each field, so name and describe them as you would for a reader.
     * <p>The answer is enforced against this schema rather than merely guided by it, so the schema
     * has to be one that can be enforced. Each rule is checked when the request arrives.</p>
     * <p><strong>Schema Requirements:</strong></p>
     * <ul>
     * <li>Root type must be &quot;object&quot; with a non-empty <code>properties</code> map</li>
     * <li>Supported types: string, number, integer, boolean, object, array, null</li>
     * <li>Structural keywords only: properties, required, additionalProperties, items, anyOf, enum, const (with a sibling type), $defs/$ref, description. Value constraints such as pattern, format, minimum or minItems, and combinators such as oneOf, allOf and if/then/else, are not accepted</li>
     * <li>Required fields: All fields must be required (no optional properties). A value that may be absent is required with a type that includes <code>null</code>, so expect every property to come back, with <code>null</code> where it does not apply</li>
     * <li>Additional properties: Must be set to false</li>
     * </ul>
     * <p><strong>Limits:</strong></p>
     * <ul>
     * <li>Maximum nesting depth: 5 levels</li>
     * <li>Maximum total properties: 100</li>
     * <li>Maximum total string length: 15,000 characters</li>
     * <li>Maximum total enum values: 500</li>
     * </ul>
     * <p>Recursive schemas are supported; a self-reference resolves against this schema. Describe
     * the object as a whole with <code>description</code> below, and annotate individual fields with their
     * own <code>description</code>.</p>
     */
    @JsonProperty("schema")
    public Object getSchema() {
        return schema;
    }

    /**
     * @return What the object as a whole is for, where a property's own <code>description</code> describes that one field. Takes precedence over a <code>description</code> at the root of the schema.
     * Keep it to a description of the object. It is not a place to put instructions for the agent or per-request context — those belong in the agent's own configuration and in <code>text</code>.
     */
    @JsonProperty("description")
    public Optional<String> getDescription() {
        return description;
    }

    @java.lang.Override
    public boolean equals(Object other) {
        if (this == other) return true;
        return other instanceof TextFormatJsonSchema && equalTo((TextFormatJsonSchema) other);
    }

    @JsonAnyGetter
    public Map<String, Object> getAdditionalProperties() {
        return this.additionalProperties;
    }

    private boolean equalTo(TextFormatJsonSchema other) {
        return schema.equals(other.schema) && description.equals(other.description);
    }

    @java.lang.Override
    public int hashCode() {
        return Objects.hash(this.schema, this.description);
    }

    @java.lang.Override
    public String toString() {
        return ObjectMappers.stringify(this);
    }

    public static SchemaStage builder() {
        return new Builder();
    }

    public interface SchemaStage {
        /**
         * <p>The required shape. Property names and their <code>description</code>s are what tell the agent how
         * to fill each field, so name and describe them as you would for a reader.</p>
         * <p>The answer is enforced against this schema rather than merely guided by it, so the schema
         * has to be one that can be enforced. Each rule is checked when the request arrives.</p>
         * <p><strong>Schema Requirements:</strong></p>
         * <ul>
         * <li>Root type must be &quot;object&quot; with a non-empty <code>properties</code> map</li>
         * <li>Supported types: string, number, integer, boolean, object, array, null</li>
         * <li>Structural keywords only: properties, required, additionalProperties, items, anyOf, enum, const (with a sibling type), $defs/$ref, description. Value constraints such as pattern, format, minimum or minItems, and combinators such as oneOf, allOf and if/then/else, are not accepted</li>
         * <li>Required fields: All fields must be required (no optional properties). A value that may be absent is required with a type that includes <code>null</code>, so expect every property to come back, with <code>null</code> where it does not apply</li>
         * <li>Additional properties: Must be set to false</li>
         * </ul>
         * <p><strong>Limits:</strong></p>
         * <ul>
         * <li>Maximum nesting depth: 5 levels</li>
         * <li>Maximum total properties: 100</li>
         * <li>Maximum total string length: 15,000 characters</li>
         * <li>Maximum total enum values: 500</li>
         * </ul>
         * <p>Recursive schemas are supported; a self-reference resolves against this schema. Describe
         * the object as a whole with <code>description</code> below, and annotate individual fields with their
         * own <code>description</code>.</p>
         */
        _FinalStage schema(Object schema);

        Builder from(TextFormatJsonSchema other);
    }

    public interface _FinalStage {
        TextFormatJsonSchema build();

        /**
         * <p>What the object as a whole is for, where a property's own <code>description</code> describes that one field. Takes precedence over a <code>description</code> at the root of the schema.
         * Keep it to a description of the object. It is not a place to put instructions for the agent or per-request context — those belong in the agent's own configuration and in <code>text</code>.</p>
         */
        _FinalStage description(Optional<String> description);

        _FinalStage description(String description);
    }

    @JsonIgnoreProperties(ignoreUnknown = true)
    public static final class Builder implements SchemaStage, _FinalStage {
        private Object schema;

        private Optional<String> description = Optional.empty();

        @JsonAnySetter
        private Map<String, Object> additionalProperties = new HashMap<>();

        private Builder() {}

        @java.lang.Override
        public Builder from(TextFormatJsonSchema other) {
            schema(other.getSchema());
            description(other.getDescription());
            return this;
        }

        /**
         * <p>The required shape. Property names and their <code>description</code>s are what tell the agent how
         * to fill each field, so name and describe them as you would for a reader.</p>
         * <p>The answer is enforced against this schema rather than merely guided by it, so the schema
         * has to be one that can be enforced. Each rule is checked when the request arrives.</p>
         * <p><strong>Schema Requirements:</strong></p>
         * <ul>
         * <li>Root type must be &quot;object&quot; with a non-empty <code>properties</code> map</li>
         * <li>Supported types: string, number, integer, boolean, object, array, null</li>
         * <li>Structural keywords only: properties, required, additionalProperties, items, anyOf, enum, const (with a sibling type), $defs/$ref, description. Value constraints such as pattern, format, minimum or minItems, and combinators such as oneOf, allOf and if/then/else, are not accepted</li>
         * <li>Required fields: All fields must be required (no optional properties). A value that may be absent is required with a type that includes <code>null</code>, so expect every property to come back, with <code>null</code> where it does not apply</li>
         * <li>Additional properties: Must be set to false</li>
         * </ul>
         * <p><strong>Limits:</strong></p>
         * <ul>
         * <li>Maximum nesting depth: 5 levels</li>
         * <li>Maximum total properties: 100</li>
         * <li>Maximum total string length: 15,000 characters</li>
         * <li>Maximum total enum values: 500</li>
         * </ul>
         * <p>Recursive schemas are supported; a self-reference resolves against this schema. Describe
         * the object as a whole with <code>description</code> below, and annotate individual fields with their
         * own <code>description</code>.</p>
         * <p>The required shape. Property names and their <code>description</code>s are what tell the agent how
         * to fill each field, so name and describe them as you would for a reader.</p>
         * <p>The answer is enforced against this schema rather than merely guided by it, so the schema
         * has to be one that can be enforced. Each rule is checked when the request arrives.</p>
         * <p><strong>Schema Requirements:</strong></p>
         * <ul>
         * <li>Root type must be &quot;object&quot; with a non-empty <code>properties</code> map</li>
         * <li>Supported types: string, number, integer, boolean, object, array, null</li>
         * <li>Structural keywords only: properties, required, additionalProperties, items, anyOf, enum, const (with a sibling type), $defs/$ref, description. Value constraints such as pattern, format, minimum or minItems, and combinators such as oneOf, allOf and if/then/else, are not accepted</li>
         * <li>Required fields: All fields must be required (no optional properties). A value that may be absent is required with a type that includes <code>null</code>, so expect every property to come back, with <code>null</code> where it does not apply</li>
         * <li>Additional properties: Must be set to false</li>
         * </ul>
         * <p><strong>Limits:</strong></p>
         * <ul>
         * <li>Maximum nesting depth: 5 levels</li>
         * <li>Maximum total properties: 100</li>
         * <li>Maximum total string length: 15,000 characters</li>
         * <li>Maximum total enum values: 500</li>
         * </ul>
         * <p>Recursive schemas are supported; a self-reference resolves against this schema. Describe
         * the object as a whole with <code>description</code> below, and annotate individual fields with their
         * own <code>description</code>.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("schema")
        public _FinalStage schema(Object schema) {
            this.schema = schema;
            return this;
        }

        /**
         * <p>What the object as a whole is for, where a property's own <code>description</code> describes that one field. Takes precedence over a <code>description</code> at the root of the schema.
         * Keep it to a description of the object. It is not a place to put instructions for the agent or per-request context — those belong in the agent's own configuration and in <code>text</code>.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage description(String description) {
            this.description = Optional.ofNullable(description);
            return this;
        }

        /**
         * <p>What the object as a whole is for, where a property's own <code>description</code> describes that one field. Takes precedence over a <code>description</code> at the root of the schema.
         * Keep it to a description of the object. It is not a place to put instructions for the agent or per-request context — those belong in the agent's own configuration and in <code>text</code>.</p>
         */
        @java.lang.Override
        @JsonSetter(value = "description", nulls = Nulls.SKIP)
        public _FinalStage description(Optional<String> description) {
            this.description = description;
            return this;
        }

        @java.lang.Override
        public TextFormatJsonSchema build() {
            return new TextFormatJsonSchema(schema, description, additionalProperties);
        }
    }
}
