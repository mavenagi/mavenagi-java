/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.mavenagi.resources.voice.types;

import com.fasterxml.jackson.annotation.JsonAnyGetter;
import com.fasterxml.jackson.annotation.JsonAnySetter;
import com.fasterxml.jackson.annotation.JsonIgnoreProperties;
import com.fasterxml.jackson.annotation.JsonInclude;
import com.fasterxml.jackson.annotation.JsonProperty;
import com.fasterxml.jackson.annotation.JsonSetter;
import com.fasterxml.jackson.annotation.Nulls;
import com.fasterxml.jackson.databind.annotation.JsonDeserialize;
import com.mavenagi.core.ObjectMappers;
import java.util.HashMap;
import java.util.Map;
import java.util.Objects;
import java.util.Optional;
import org.jetbrains.annotations.NotNull;

@JsonInclude(JsonInclude.Include.NON_ABSENT)
@JsonDeserialize(builder = VoiceSessionTokenRequest.Builder.class)
public final class VoiceSessionTokenRequest {
    private final String appUserId;

    private final VoiceTokenType type;

    private final Optional<Map<String, String>> customData;

    private final Map<String, Object> additionalProperties;

    private VoiceSessionTokenRequest(
            String appUserId,
            VoiceTokenType type,
            Optional<Map<String, String>> customData,
            Map<String, Object> additionalProperties) {
        this.appUserId = appUserId;
        this.type = type;
        this.customData = customData;
        this.additionalProperties = additionalProperties;
    }

    /**
     * @return The end user identity for the voice session.
     */
    @JsonProperty("appUserId")
    public String getAppUserId() {
        return appUserId;
    }

    /**
     * @return The type of session token to generate.
     * Use &quot;webrtc&quot; for browser-based calls via Twilio, or &quot;websocket&quot; for direct WebSocket connections.
     */
    @JsonProperty("type")
    public VoiceTokenType getType() {
        return type;
    }

    /**
     * @return Arbitrary key-value metadata to associate with this session (e.g., conversationId, topic).
     * For WebRTC tokens, this data is stored server-side and referenced by a secure ID
     * encoded in the token identity, ensuring it cannot be tampered with by the client.
     * For WebSocket tokens, clients can also pass data directly in the Config message.
     */
    @JsonProperty("customData")
    public Optional<Map<String, String>> getCustomData() {
        return customData;
    }

    @java.lang.Override
    public boolean equals(Object other) {
        if (this == other) return true;
        return other instanceof VoiceSessionTokenRequest && equalTo((VoiceSessionTokenRequest) other);
    }

    @JsonAnyGetter
    public Map<String, Object> getAdditionalProperties() {
        return this.additionalProperties;
    }

    private boolean equalTo(VoiceSessionTokenRequest other) {
        return appUserId.equals(other.appUserId) && type.equals(other.type) && customData.equals(other.customData);
    }

    @java.lang.Override
    public int hashCode() {
        return Objects.hash(this.appUserId, this.type, this.customData);
    }

    @java.lang.Override
    public String toString() {
        return ObjectMappers.stringify(this);
    }

    public static AppUserIdStage builder() {
        return new Builder();
    }

    public interface AppUserIdStage {
        /**
         * <p>The end user identity for the voice session.</p>
         */
        TypeStage appUserId(@NotNull String appUserId);

        Builder from(VoiceSessionTokenRequest other);
    }

    public interface TypeStage {
        /**
         * <p>The type of session token to generate.
         * Use &quot;webrtc&quot; for browser-based calls via Twilio, or &quot;websocket&quot; for direct WebSocket connections.</p>
         */
        _FinalStage type(@NotNull VoiceTokenType type);
    }

    public interface _FinalStage {
        VoiceSessionTokenRequest build();

        /**
         * <p>Arbitrary key-value metadata to associate with this session (e.g., conversationId, topic).
         * For WebRTC tokens, this data is stored server-side and referenced by a secure ID
         * encoded in the token identity, ensuring it cannot be tampered with by the client.
         * For WebSocket tokens, clients can also pass data directly in the Config message.</p>
         */
        _FinalStage customData(Optional<Map<String, String>> customData);

        _FinalStage customData(Map<String, String> customData);
    }

    @JsonIgnoreProperties(ignoreUnknown = true)
    public static final class Builder implements AppUserIdStage, TypeStage, _FinalStage {
        private String appUserId;

        private VoiceTokenType type;

        private Optional<Map<String, String>> customData = Optional.empty();

        @JsonAnySetter
        private Map<String, Object> additionalProperties = new HashMap<>();

        private Builder() {}

        @java.lang.Override
        public Builder from(VoiceSessionTokenRequest other) {
            appUserId(other.getAppUserId());
            type(other.getType());
            customData(other.getCustomData());
            return this;
        }

        /**
         * <p>The end user identity for the voice session.</p>
         * <p>The end user identity for the voice session.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("appUserId")
        public TypeStage appUserId(@NotNull String appUserId) {
            this.appUserId = Objects.requireNonNull(appUserId, "appUserId must not be null");
            return this;
        }

        /**
         * <p>The type of session token to generate.
         * Use &quot;webrtc&quot; for browser-based calls via Twilio, or &quot;websocket&quot; for direct WebSocket connections.</p>
         * <p>The type of session token to generate.
         * Use &quot;webrtc&quot; for browser-based calls via Twilio, or &quot;websocket&quot; for direct WebSocket connections.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("type")
        public _FinalStage type(@NotNull VoiceTokenType type) {
            this.type = Objects.requireNonNull(type, "type must not be null");
            return this;
        }

        /**
         * <p>Arbitrary key-value metadata to associate with this session (e.g., conversationId, topic).
         * For WebRTC tokens, this data is stored server-side and referenced by a secure ID
         * encoded in the token identity, ensuring it cannot be tampered with by the client.
         * For WebSocket tokens, clients can also pass data directly in the Config message.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        public _FinalStage customData(Map<String, String> customData) {
            this.customData = Optional.ofNullable(customData);
            return this;
        }

        /**
         * <p>Arbitrary key-value metadata to associate with this session (e.g., conversationId, topic).
         * For WebRTC tokens, this data is stored server-side and referenced by a secure ID
         * encoded in the token identity, ensuring it cannot be tampered with by the client.
         * For WebSocket tokens, clients can also pass data directly in the Config message.</p>
         */
        @java.lang.Override
        @JsonSetter(value = "customData", nulls = Nulls.SKIP)
        public _FinalStage customData(Optional<Map<String, String>> customData) {
            this.customData = customData;
            return this;
        }

        @java.lang.Override
        public VoiceSessionTokenRequest build() {
            return new VoiceSessionTokenRequest(appUserId, type, customData, additionalProperties);
        }
    }
}
