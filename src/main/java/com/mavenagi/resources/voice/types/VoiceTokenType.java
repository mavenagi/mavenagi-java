/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.mavenagi.resources.voice.types;

import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonValue;

public final class VoiceTokenType {
    public static final VoiceTokenType WEBSOCKET = new VoiceTokenType(Value.WEBSOCKET, "websocket");

    public static final VoiceTokenType WEBRTC = new VoiceTokenType(Value.WEBRTC, "webrtc");

    private final Value value;

    private final String string;

    VoiceTokenType(Value value, String string) {
        this.value = value;
        this.string = string;
    }

    public Value getEnumValue() {
        return value;
    }

    @java.lang.Override
    @JsonValue
    public String toString() {
        return this.string;
    }

    @java.lang.Override
    public boolean equals(Object other) {
        return (this == other)
                || (other instanceof VoiceTokenType && this.string.equals(((VoiceTokenType) other).string));
    }

    @java.lang.Override
    public int hashCode() {
        return this.string.hashCode();
    }

    public <T> T visit(Visitor<T> visitor) {
        switch (value) {
            case WEBSOCKET:
                return visitor.visitWebsocket();
            case WEBRTC:
                return visitor.visitWebrtc();
            case UNKNOWN:
            default:
                return visitor.visitUnknown(string);
        }
    }

    @JsonCreator(mode = JsonCreator.Mode.DELEGATING)
    public static VoiceTokenType valueOf(String value) {
        switch (value) {
            case "websocket":
                return WEBSOCKET;
            case "webrtc":
                return WEBRTC;
            default:
                return new VoiceTokenType(Value.UNKNOWN, value);
        }
    }

    public enum Value {
        WEBRTC,

        WEBSOCKET,

        UNKNOWN
    }

    public interface Visitor<T> {
        T visitWebrtc();

        T visitWebsocket();

        T visitUnknown(String unknownType);
    }
}
