/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.mavenagi.resources.commons.types;

import com.fasterxml.jackson.annotation.JsonAnyGetter;
import com.fasterxml.jackson.annotation.JsonAnySetter;
import com.fasterxml.jackson.annotation.JsonIgnoreProperties;
import com.fasterxml.jackson.annotation.JsonInclude;
import com.fasterxml.jackson.annotation.JsonProperty;
import com.fasterxml.jackson.annotation.JsonSetter;
import com.fasterxml.jackson.annotation.Nulls;
import com.fasterxml.jackson.databind.annotation.JsonDeserialize;
import com.mavenagi.core.ObjectMappers;
import java.util.HashMap;
import java.util.LinkedHashSet;
import java.util.Map;
import java.util.Objects;
import java.util.Set;

@JsonInclude(JsonInclude.Include.NON_ABSENT)
@JsonDeserialize(builder = KnowledgeContextByEntities.Builder.class)
public final class KnowledgeContextByEntities {
    private final Set<ScopedEntity> entities;

    private final Map<String, Object> additionalProperties;

    private KnowledgeContextByEntities(Set<ScopedEntity> entities, Map<String, Object> additionalProperties) {
        this.entities = entities;
        this.additionalProperties = additionalProperties;
    }

    /**
     * @return The entities whose documents should be retrievable, in addition to the agent's own
     * knowledge. Each <code>entityId</code> must be fully specified and must belong to the organization
     * and agent the request is made against; one that does not is rejected. Entities are never
     * silently dropped, which would omit exactly the documents the caller asked to bring into
     * scope.
     * <p><code>AGENT</code> and <code>FEEDBACK</code> are rejected: neither has an internal form to resolve to, and
     * <code>AGENT</code> in particular is redundant here because the agent's own knowledge is always in
     * scope. Every other <code>EntityType</code> is accepted.</p>
     */
    @JsonProperty("entities")
    public Set<ScopedEntity> getEntities() {
        return entities;
    }

    @java.lang.Override
    public boolean equals(Object other) {
        if (this == other) return true;
        return other instanceof KnowledgeContextByEntities && equalTo((KnowledgeContextByEntities) other);
    }

    @JsonAnyGetter
    public Map<String, Object> getAdditionalProperties() {
        return this.additionalProperties;
    }

    private boolean equalTo(KnowledgeContextByEntities other) {
        return entities.equals(other.entities);
    }

    @java.lang.Override
    public int hashCode() {
        return Objects.hash(this.entities);
    }

    @java.lang.Override
    public String toString() {
        return ObjectMappers.stringify(this);
    }

    public static Builder builder() {
        return new Builder();
    }

    @JsonIgnoreProperties(ignoreUnknown = true)
    public static final class Builder {
        private Set<ScopedEntity> entities = new LinkedHashSet<>();

        @JsonAnySetter
        private Map<String, Object> additionalProperties = new HashMap<>();

        private Builder() {}

        public Builder from(KnowledgeContextByEntities other) {
            entities(other.getEntities());
            return this;
        }

        /**
         * <p>The entities whose documents should be retrievable, in addition to the agent's own
         * knowledge. Each <code>entityId</code> must be fully specified and must belong to the organization
         * and agent the request is made against; one that does not is rejected. Entities are never
         * silently dropped, which would omit exactly the documents the caller asked to bring into
         * scope.</p>
         * <p><code>AGENT</code> and <code>FEEDBACK</code> are rejected: neither has an internal form to resolve to, and
         * <code>AGENT</code> in particular is redundant here because the agent's own knowledge is always in
         * scope. Every other <code>EntityType</code> is accepted.</p>
         */
        @JsonSetter(value = "entities", nulls = Nulls.SKIP)
        public Builder entities(Set<ScopedEntity> entities) {
            this.entities.clear();
            if (entities != null) {
                this.entities.addAll(entities);
            }
            return this;
        }

        public Builder addEntities(ScopedEntity entities) {
            this.entities.add(entities);
            return this;
        }

        public Builder addAllEntities(Set<ScopedEntity> entities) {
            if (entities != null) {
                this.entities.addAll(entities);
            }
            return this;
        }

        public KnowledgeContextByEntities build() {
            return new KnowledgeContextByEntities(entities, additionalProperties);
        }
    }
}
