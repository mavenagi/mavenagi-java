/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.mavenagi.resources.commons.types;

import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonIgnoreProperties;
import com.fasterxml.jackson.annotation.JsonProperty;
import com.fasterxml.jackson.annotation.JsonSubTypes;
import com.fasterxml.jackson.annotation.JsonTypeInfo;
import com.fasterxml.jackson.annotation.JsonTypeName;
import com.fasterxml.jackson.annotation.JsonUnwrapped;
import com.fasterxml.jackson.annotation.JsonValue;
import java.util.Objects;
import java.util.Optional;

public final class IntelligentFieldCondition {
    private final Value value;

    @JsonCreator(mode = JsonCreator.Mode.DELEGATING)
    private IntelligentFieldCondition(Value value) {
        this.value = value;
    }

    public <T> T visit(Visitor<T> visitor) {
        return value.visit(visitor);
    }

    public static IntelligentFieldCondition string(StringCondition value) {
        return new IntelligentFieldCondition(new StringValue(value));
    }

    public static IntelligentFieldCondition numeric(NumericCondition value) {
        return new IntelligentFieldCondition(new NumericValue(value));
    }

    public static IntelligentFieldCondition boolean_(BooleanCondition value) {
        return new IntelligentFieldCondition(new BooleanValue(value));
    }

    public static IntelligentFieldCondition set(SetCondition value) {
        return new IntelligentFieldCondition(new SetValue(value));
    }

    public boolean isString() {
        return value instanceof StringValue;
    }

    public boolean isNumeric() {
        return value instanceof NumericValue;
    }

    public boolean isBoolean() {
        return value instanceof BooleanValue;
    }

    public boolean isSet() {
        return value instanceof SetValue;
    }

    public boolean _isUnknown() {
        return value instanceof _UnknownValue;
    }

    public Optional<StringCondition> getString() {
        if (isString()) {
            return Optional.of(((StringValue) value).value);
        }
        return Optional.empty();
    }

    public Optional<NumericCondition> getNumeric() {
        if (isNumeric()) {
            return Optional.of(((NumericValue) value).value);
        }
        return Optional.empty();
    }

    public Optional<BooleanCondition> getBoolean() {
        if (isBoolean()) {
            return Optional.of(((BooleanValue) value).value);
        }
        return Optional.empty();
    }

    public Optional<SetCondition> getSet() {
        if (isSet()) {
            return Optional.of(((SetValue) value).value);
        }
        return Optional.empty();
    }

    public Optional<Object> _getUnknown() {
        if (_isUnknown()) {
            return Optional.of(((_UnknownValue) value).value);
        }
        return Optional.empty();
    }

    @JsonValue
    private Value getValue() {
        return this.value;
    }

    public interface Visitor<T> {
        T visitString(StringCondition string);

        T visitNumeric(NumericCondition numeric);

        T visitBoolean(BooleanCondition boolean_);

        T visitSet(SetCondition set);

        T _visitUnknown(Object unknownType);
    }

    @JsonTypeInfo(
            use = JsonTypeInfo.Id.NAME,
            property = "fieldValidationType",
            visible = true,
            defaultImpl = _UnknownValue.class)
    @JsonSubTypes({
        @JsonSubTypes.Type(StringValue.class),
        @JsonSubTypes.Type(NumericValue.class),
        @JsonSubTypes.Type(BooleanValue.class),
        @JsonSubTypes.Type(SetValue.class)
    })
    @JsonIgnoreProperties(ignoreUnknown = true)
    private interface Value {
        <T> T visit(Visitor<T> visitor);
    }

    @JsonTypeName("string")
    @JsonIgnoreProperties("fieldValidationType")
    private static final class StringValue implements Value {
        @JsonProperty("value")
        private StringCondition value;

        @JsonCreator(mode = JsonCreator.Mode.PROPERTIES)
        private StringValue(@JsonProperty("value") StringCondition value) {
            this.value = value;
        }

        @java.lang.Override
        public <T> T visit(Visitor<T> visitor) {
            return visitor.visitString(value);
        }

        @java.lang.Override
        public boolean equals(Object other) {
            if (this == other) return true;
            return other instanceof StringValue && equalTo((StringValue) other);
        }

        private boolean equalTo(StringValue other) {
            return value.equals(other.value);
        }

        @java.lang.Override
        public int hashCode() {
            return Objects.hash(this.value);
        }

        @java.lang.Override
        public String toString() {
            return "IntelligentFieldCondition{" + "value: " + value + "}";
        }
    }

    @JsonTypeName("numeric")
    @JsonIgnoreProperties("fieldValidationType")
    private static final class NumericValue implements Value {
        @JsonProperty("value")
        private NumericCondition value;

        @JsonCreator(mode = JsonCreator.Mode.PROPERTIES)
        private NumericValue(@JsonProperty("value") NumericCondition value) {
            this.value = value;
        }

        @java.lang.Override
        public <T> T visit(Visitor<T> visitor) {
            return visitor.visitNumeric(value);
        }

        @java.lang.Override
        public boolean equals(Object other) {
            if (this == other) return true;
            return other instanceof NumericValue && equalTo((NumericValue) other);
        }

        private boolean equalTo(NumericValue other) {
            return value.equals(other.value);
        }

        @java.lang.Override
        public int hashCode() {
            return Objects.hash(this.value);
        }

        @java.lang.Override
        public String toString() {
            return "IntelligentFieldCondition{" + "value: " + value + "}";
        }
    }

    @JsonTypeName("boolean")
    @JsonIgnoreProperties("fieldValidationType")
    private static final class BooleanValue implements Value {
        @JsonUnwrapped
        private BooleanCondition value;

        @JsonCreator(mode = JsonCreator.Mode.PROPERTIES)
        private BooleanValue() {}

        private BooleanValue(BooleanCondition value) {
            this.value = value;
        }

        @java.lang.Override
        public <T> T visit(Visitor<T> visitor) {
            return visitor.visitBoolean(value);
        }

        @java.lang.Override
        public boolean equals(Object other) {
            if (this == other) return true;
            return other instanceof BooleanValue && equalTo((BooleanValue) other);
        }

        private boolean equalTo(BooleanValue other) {
            return value.equals(other.value);
        }

        @java.lang.Override
        public int hashCode() {
            return Objects.hash(this.value);
        }

        @java.lang.Override
        public String toString() {
            return "IntelligentFieldCondition{" + "value: " + value + "}";
        }
    }

    @JsonTypeName("set")
    @JsonIgnoreProperties("fieldValidationType")
    private static final class SetValue implements Value {
        @JsonProperty("value")
        private SetCondition value;

        @JsonCreator(mode = JsonCreator.Mode.PROPERTIES)
        private SetValue(@JsonProperty("value") SetCondition value) {
            this.value = value;
        }

        @java.lang.Override
        public <T> T visit(Visitor<T> visitor) {
            return visitor.visitSet(value);
        }

        @java.lang.Override
        public boolean equals(Object other) {
            if (this == other) return true;
            return other instanceof SetValue && equalTo((SetValue) other);
        }

        private boolean equalTo(SetValue other) {
            return value.equals(other.value);
        }

        @java.lang.Override
        public int hashCode() {
            return Objects.hash(this.value);
        }

        @java.lang.Override
        public String toString() {
            return "IntelligentFieldCondition{" + "value: " + value + "}";
        }
    }

    @JsonIgnoreProperties("fieldValidationType")
    private static final class _UnknownValue implements Value {
        private String type;

        @JsonValue
        private Object value;

        @JsonCreator(mode = JsonCreator.Mode.PROPERTIES)
        private _UnknownValue(@JsonProperty("value") Object value) {}

        @java.lang.Override
        public <T> T visit(Visitor<T> visitor) {
            return visitor._visitUnknown(value);
        }

        @java.lang.Override
        public boolean equals(Object other) {
            if (this == other) return true;
            return other instanceof _UnknownValue && equalTo((_UnknownValue) other);
        }

        private boolean equalTo(_UnknownValue other) {
            return type.equals(other.type) && value.equals(other.value);
        }

        @java.lang.Override
        public int hashCode() {
            return Objects.hash(this.type, this.value);
        }

        @java.lang.Override
        public String toString() {
            return "IntelligentFieldCondition{" + "type: " + type + ", value: " + value + "}";
        }
    }
}
