/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.mavenagi.resources.knowledge.types;

import com.fasterxml.jackson.annotation.JsonAnyGetter;
import com.fasterxml.jackson.annotation.JsonAnySetter;
import com.fasterxml.jackson.annotation.JsonIgnoreProperties;
import com.fasterxml.jackson.annotation.JsonInclude;
import com.fasterxml.jackson.annotation.JsonProperty;
import com.fasterxml.jackson.annotation.JsonSetter;
import com.fasterxml.jackson.databind.annotation.JsonDeserialize;
import com.mavenagi.core.ObjectMappers;
import com.mavenagi.resources.commons.types.EntityId;
import java.util.HashMap;
import java.util.Map;
import java.util.Objects;
import org.jetbrains.annotations.NotNull;

@JsonInclude(JsonInclude.Include.NON_ABSENT)
@JsonDeserialize(builder = KnowledgeBaseIndexingProgressState.Builder.class)
public final class KnowledgeBaseIndexingProgressState {
    private final EntityId versionId;

    private final KnowledgeBaseIndexingStatus status;

    private final long expectedCount;

    private final long indexedCount;

    private final long failedCount;

    private final Map<String, Object> additionalProperties;

    private KnowledgeBaseIndexingProgressState(
            EntityId versionId,
            KnowledgeBaseIndexingStatus status,
            long expectedCount,
            long indexedCount,
            long failedCount,
            Map<String, Object> additionalProperties) {
        this.versionId = versionId;
        this.status = status;
        this.expectedCount = expectedCount;
        this.indexedCount = indexedCount;
        this.failedCount = failedCount;
        this.additionalProperties = additionalProperties;
    }

    /**
     * @return The unique ID of a knowledge base version.
     */
    @JsonProperty("versionId")
    public EntityId getVersionId() {
        return versionId;
    }

    /**
     * @return The status of the knowledge base indexing process.
     */
    @JsonProperty("status")
    public KnowledgeBaseIndexingStatus getStatus() {
        return status;
    }

    /**
     * @return The expected number of documents to be indexed for the latest knowledge base version.
     */
    @JsonProperty("expectedCount")
    public long getExpectedCount() {
        return expectedCount;
    }

    /**
     * @return The number of documents that have been indexed so far for the latest knowledge base version.
     */
    @JsonProperty("indexedCount")
    public long getIndexedCount() {
        return indexedCount;
    }

    /**
     * @return The number of documents that have failed to index for the latest knowledge base version.
     */
    @JsonProperty("failedCount")
    public long getFailedCount() {
        return failedCount;
    }

    @java.lang.Override
    public boolean equals(Object other) {
        if (this == other) return true;
        return other instanceof KnowledgeBaseIndexingProgressState
                && equalTo((KnowledgeBaseIndexingProgressState) other);
    }

    @JsonAnyGetter
    public Map<String, Object> getAdditionalProperties() {
        return this.additionalProperties;
    }

    private boolean equalTo(KnowledgeBaseIndexingProgressState other) {
        return versionId.equals(other.versionId)
                && status.equals(other.status)
                && expectedCount == other.expectedCount
                && indexedCount == other.indexedCount
                && failedCount == other.failedCount;
    }

    @java.lang.Override
    public int hashCode() {
        return Objects.hash(this.versionId, this.status, this.expectedCount, this.indexedCount, this.failedCount);
    }

    @java.lang.Override
    public String toString() {
        return ObjectMappers.stringify(this);
    }

    public static VersionIdStage builder() {
        return new Builder();
    }

    public interface VersionIdStage {
        /**
         * <p>The unique ID of a knowledge base version.</p>
         */
        StatusStage versionId(@NotNull EntityId versionId);

        Builder from(KnowledgeBaseIndexingProgressState other);
    }

    public interface StatusStage {
        /**
         * <p>The status of the knowledge base indexing process.</p>
         */
        ExpectedCountStage status(@NotNull KnowledgeBaseIndexingStatus status);
    }

    public interface ExpectedCountStage {
        /**
         * <p>The expected number of documents to be indexed for the latest knowledge base version.</p>
         */
        IndexedCountStage expectedCount(long expectedCount);
    }

    public interface IndexedCountStage {
        /**
         * <p>The number of documents that have been indexed so far for the latest knowledge base version.</p>
         */
        FailedCountStage indexedCount(long indexedCount);
    }

    public interface FailedCountStage {
        /**
         * <p>The number of documents that have failed to index for the latest knowledge base version.</p>
         */
        _FinalStage failedCount(long failedCount);
    }

    public interface _FinalStage {
        KnowledgeBaseIndexingProgressState build();
    }

    @JsonIgnoreProperties(ignoreUnknown = true)
    public static final class Builder
            implements VersionIdStage,
                    StatusStage,
                    ExpectedCountStage,
                    IndexedCountStage,
                    FailedCountStage,
                    _FinalStage {
        private EntityId versionId;

        private KnowledgeBaseIndexingStatus status;

        private long expectedCount;

        private long indexedCount;

        private long failedCount;

        @JsonAnySetter
        private Map<String, Object> additionalProperties = new HashMap<>();

        private Builder() {}

        @java.lang.Override
        public Builder from(KnowledgeBaseIndexingProgressState other) {
            versionId(other.getVersionId());
            status(other.getStatus());
            expectedCount(other.getExpectedCount());
            indexedCount(other.getIndexedCount());
            failedCount(other.getFailedCount());
            return this;
        }

        /**
         * <p>The unique ID of a knowledge base version.</p>
         * <p>The unique ID of a knowledge base version.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("versionId")
        public StatusStage versionId(@NotNull EntityId versionId) {
            this.versionId = Objects.requireNonNull(versionId, "versionId must not be null");
            return this;
        }

        /**
         * <p>The status of the knowledge base indexing process.</p>
         * <p>The status of the knowledge base indexing process.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("status")
        public ExpectedCountStage status(@NotNull KnowledgeBaseIndexingStatus status) {
            this.status = Objects.requireNonNull(status, "status must not be null");
            return this;
        }

        /**
         * <p>The expected number of documents to be indexed for the latest knowledge base version.</p>
         * <p>The expected number of documents to be indexed for the latest knowledge base version.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("expectedCount")
        public IndexedCountStage expectedCount(long expectedCount) {
            this.expectedCount = expectedCount;
            return this;
        }

        /**
         * <p>The number of documents that have been indexed so far for the latest knowledge base version.</p>
         * <p>The number of documents that have been indexed so far for the latest knowledge base version.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("indexedCount")
        public FailedCountStage indexedCount(long indexedCount) {
            this.indexedCount = indexedCount;
            return this;
        }

        /**
         * <p>The number of documents that have failed to index for the latest knowledge base version.</p>
         * <p>The number of documents that have failed to index for the latest knowledge base version.</p>
         * @return Reference to {@code this} so that method calls can be chained together.
         */
        @java.lang.Override
        @JsonSetter("failedCount")
        public _FinalStage failedCount(long failedCount) {
            this.failedCount = failedCount;
            return this;
        }

        @java.lang.Override
        public KnowledgeBaseIndexingProgressState build() {
            return new KnowledgeBaseIndexingProgressState(
                    versionId, status, expectedCount, indexedCount, failedCount, additionalProperties);
        }
    }
}
