/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.mavenagi.resources.commons.types;

import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonIgnoreProperties;
import com.fasterxml.jackson.annotation.JsonProperty;
import com.fasterxml.jackson.annotation.JsonSubTypes;
import com.fasterxml.jackson.annotation.JsonTypeInfo;
import com.fasterxml.jackson.annotation.JsonTypeName;
import com.fasterxml.jackson.annotation.JsonUnwrapped;
import com.fasterxml.jackson.annotation.JsonValue;
import java.util.Objects;
import java.util.Optional;

public final class SetCondition {
    private final Value value;

    @JsonCreator(mode = JsonCreator.Mode.DELEGATING)
    private SetCondition(Value value) {
        this.value = value;
    }

    public <T> T visit(Visitor<T> visitor) {
        return value.visit(visitor);
    }

    public static SetCondition element(SetElementCondition value) {
        return new SetCondition(new ElementValue(value));
    }

    public static SetCondition relation(SetRelationCondition value) {
        return new SetCondition(new RelationValue(value));
    }

    public boolean isElement() {
        return value instanceof ElementValue;
    }

    public boolean isRelation() {
        return value instanceof RelationValue;
    }

    public boolean _isUnknown() {
        return value instanceof _UnknownValue;
    }

    public Optional<SetElementCondition> getElement() {
        if (isElement()) {
            return Optional.of(((ElementValue) value).value);
        }
        return Optional.empty();
    }

    public Optional<SetRelationCondition> getRelation() {
        if (isRelation()) {
            return Optional.of(((RelationValue) value).value);
        }
        return Optional.empty();
    }

    public Optional<Object> _getUnknown() {
        if (_isUnknown()) {
            return Optional.of(((_UnknownValue) value).value);
        }
        return Optional.empty();
    }

    @JsonValue
    private Value getValue() {
        return this.value;
    }

    public interface Visitor<T> {
        T visitElement(SetElementCondition element);

        T visitRelation(SetRelationCondition relation);

        T _visitUnknown(Object unknownType);
    }

    @JsonTypeInfo(use = JsonTypeInfo.Id.NAME, property = "opType", visible = true, defaultImpl = _UnknownValue.class)
    @JsonSubTypes({@JsonSubTypes.Type(ElementValue.class), @JsonSubTypes.Type(RelationValue.class)})
    @JsonIgnoreProperties(ignoreUnknown = true)
    private interface Value {
        <T> T visit(Visitor<T> visitor);
    }

    @JsonTypeName("element")
    @JsonIgnoreProperties("opType")
    private static final class ElementValue implements Value {
        @JsonUnwrapped
        private SetElementCondition value;

        @JsonCreator(mode = JsonCreator.Mode.PROPERTIES)
        private ElementValue() {}

        private ElementValue(SetElementCondition value) {
            this.value = value;
        }

        @java.lang.Override
        public <T> T visit(Visitor<T> visitor) {
            return visitor.visitElement(value);
        }

        @java.lang.Override
        public boolean equals(Object other) {
            if (this == other) return true;
            return other instanceof ElementValue && equalTo((ElementValue) other);
        }

        private boolean equalTo(ElementValue other) {
            return value.equals(other.value);
        }

        @java.lang.Override
        public int hashCode() {
            return Objects.hash(this.value);
        }

        @java.lang.Override
        public String toString() {
            return "SetCondition{" + "value: " + value + "}";
        }
    }

    @JsonTypeName("relation")
    @JsonIgnoreProperties("opType")
    private static final class RelationValue implements Value {
        @JsonUnwrapped
        private SetRelationCondition value;

        @JsonCreator(mode = JsonCreator.Mode.PROPERTIES)
        private RelationValue() {}

        private RelationValue(SetRelationCondition value) {
            this.value = value;
        }

        @java.lang.Override
        public <T> T visit(Visitor<T> visitor) {
            return visitor.visitRelation(value);
        }

        @java.lang.Override
        public boolean equals(Object other) {
            if (this == other) return true;
            return other instanceof RelationValue && equalTo((RelationValue) other);
        }

        private boolean equalTo(RelationValue other) {
            return value.equals(other.value);
        }

        @java.lang.Override
        public int hashCode() {
            return Objects.hash(this.value);
        }

        @java.lang.Override
        public String toString() {
            return "SetCondition{" + "value: " + value + "}";
        }
    }

    @JsonIgnoreProperties("opType")
    private static final class _UnknownValue implements Value {
        private String type;

        @JsonValue
        private Object value;

        @JsonCreator(mode = JsonCreator.Mode.PROPERTIES)
        private _UnknownValue(@JsonProperty("value") Object value) {}

        @java.lang.Override
        public <T> T visit(Visitor<T> visitor) {
            return visitor._visitUnknown(value);
        }

        @java.lang.Override
        public boolean equals(Object other) {
            if (this == other) return true;
            return other instanceof _UnknownValue && equalTo((_UnknownValue) other);
        }

        private boolean equalTo(_UnknownValue other) {
            return type.equals(other.type) && value.equals(other.value);
        }

        @java.lang.Override
        public int hashCode() {
            return Objects.hash(this.type, this.value);
        }

        @java.lang.Override
        public String toString() {
            return "SetCondition{" + "type: " + type + ", value: " + value + "}";
        }
    }
}
