/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.mavenagi.resources.commons.types;

import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonValue;

public final class ConversationState {
    public static final ConversationState WELCOME = new ConversationState(Value.WELCOME, "WELCOME");

    private final Value value;

    private final String string;

    ConversationState(Value value, String string) {
        this.value = value;
        this.string = string;
    }

    public Value getEnumValue() {
        return value;
    }

    @java.lang.Override
    @JsonValue
    public String toString() {
        return this.string;
    }

    @java.lang.Override
    public boolean equals(Object other) {
        return (this == other)
                || (other instanceof ConversationState && this.string.equals(((ConversationState) other).string));
    }

    @java.lang.Override
    public int hashCode() {
        return this.string.hashCode();
    }

    public <T> T visit(Visitor<T> visitor) {
        switch (value) {
            case WELCOME:
                return visitor.visitWelcome();
            case UNKNOWN:
            default:
                return visitor.visitUnknown(string);
        }
    }

    @JsonCreator(mode = JsonCreator.Mode.DELEGATING)
    public static ConversationState valueOf(String value) {
        switch (value) {
            case "WELCOME":
                return WELCOME;
            default:
                return new ConversationState(Value.UNKNOWN, value);
        }
    }

    public enum Value {
        WELCOME,

        UNKNOWN
    }

    public interface Visitor<T> {
        T visitWelcome();

        T visitUnknown(String unknownType);
    }
}
