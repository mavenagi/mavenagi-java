/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.mavenagi.resources.segments;

import com.mavenagi.core.ClientOptions;
import com.mavenagi.core.RequestOptions;
import com.mavenagi.resources.segments.requests.SegmentDeleteRequest;
import com.mavenagi.resources.segments.requests.SegmentGetRequest;
import com.mavenagi.resources.segments.types.SegmentPatchRequest;
import com.mavenagi.resources.segments.types.SegmentRequest;
import com.mavenagi.resources.segments.types.SegmentResponse;
import com.mavenagi.resources.segments.types.SegmentsSearchRequest;
import com.mavenagi.resources.segments.types.SegmentsSearchResponse;

public class SegmentsClient {
    protected final ClientOptions clientOptions;

    private final RawSegmentsClient rawClient;

    public SegmentsClient(ClientOptions clientOptions) {
        this.clientOptions = clientOptions;
        this.rawClient = new RawSegmentsClient(clientOptions);
    }

    /**
     * Get responses with HTTP metadata like headers
     */
    public RawSegmentsClient withRawResponse() {
        return this.rawClient;
    }

    public SegmentsSearchResponse search() {
        return this.rawClient.search().body();
    }

    public SegmentsSearchResponse search(SegmentsSearchRequest request) {
        return this.rawClient.search(request).body();
    }

    public SegmentsSearchResponse search(SegmentsSearchRequest request, RequestOptions requestOptions) {
        return this.rawClient.search(request, requestOptions).body();
    }

    /**
     * Update a segment or create it if it doesn't exist.
     */
    public SegmentResponse createOrUpdate(SegmentRequest request) {
        return this.rawClient.createOrUpdate(request).body();
    }

    /**
     * Update a segment or create it if it doesn't exist.
     */
    public SegmentResponse createOrUpdate(SegmentRequest request, RequestOptions requestOptions) {
        return this.rawClient.createOrUpdate(request, requestOptions).body();
    }

    /**
     * Get a segment by its supplied ID
     */
    public SegmentResponse get(String segmentReferenceId) {
        return this.rawClient.get(segmentReferenceId).body();
    }

    /**
     * Get a segment by its supplied ID
     */
    public SegmentResponse get(String segmentReferenceId, SegmentGetRequest request) {
        return this.rawClient.get(segmentReferenceId, request).body();
    }

    /**
     * Get a segment by its supplied ID
     */
    public SegmentResponse get(String segmentReferenceId, SegmentGetRequest request, RequestOptions requestOptions) {
        return this.rawClient.get(segmentReferenceId, request, requestOptions).body();
    }

    /**
     * Update mutable segment fields
     * <p>The <code>appId</code> field can be provided to update a segment owned by a different app.
     * All other fields will overwrite the existing value on the segment only if provided.</p>
     */
    public SegmentResponse patch(String segmentReferenceId) {
        return this.rawClient.patch(segmentReferenceId).body();
    }

    /**
     * Update mutable segment fields
     * <p>The <code>appId</code> field can be provided to update a segment owned by a different app.
     * All other fields will overwrite the existing value on the segment only if provided.</p>
     */
    public SegmentResponse patch(String segmentReferenceId, SegmentPatchRequest request) {
        return this.rawClient.patch(segmentReferenceId, request).body();
    }

    /**
     * Update mutable segment fields
     * <p>The <code>appId</code> field can be provided to update a segment owned by a different app.
     * All other fields will overwrite the existing value on the segment only if provided.</p>
     */
    public SegmentResponse patch(
            String segmentReferenceId, SegmentPatchRequest request, RequestOptions requestOptions) {
        return this.rawClient.patch(segmentReferenceId, request, requestOptions).body();
    }

    /**
     * Soft delete a segment. Only INACTIVE segments can be deleted.
     * <p>Deleted segments are excluded from search results but can still be retrieved by ID for archival purposes. Creating a new segment with the same referenceId as a deleted segment will overwrite the deleted segment and restore it to ACTIVE status.</p>
     * <p>Deleted segments cannot be modified.</p>
     */
    public SegmentResponse delete(String segmentReferenceId) {
        return this.rawClient.delete(segmentReferenceId).body();
    }

    /**
     * Soft delete a segment. Only INACTIVE segments can be deleted.
     * <p>Deleted segments are excluded from search results but can still be retrieved by ID for archival purposes. Creating a new segment with the same referenceId as a deleted segment will overwrite the deleted segment and restore it to ACTIVE status.</p>
     * <p>Deleted segments cannot be modified.</p>
     */
    public SegmentResponse delete(String segmentReferenceId, SegmentDeleteRequest request) {
        return this.rawClient.delete(segmentReferenceId, request).body();
    }

    /**
     * Soft delete a segment. Only INACTIVE segments can be deleted.
     * <p>Deleted segments are excluded from search results but can still be retrieved by ID for archival purposes. Creating a new segment with the same referenceId as a deleted segment will overwrite the deleted segment and restore it to ACTIVE status.</p>
     * <p>Deleted segments cannot be modified.</p>
     */
    public SegmentResponse delete(
            String segmentReferenceId, SegmentDeleteRequest request, RequestOptions requestOptions) {
        return this.rawClient
                .delete(segmentReferenceId, request, requestOptions)
                .body();
    }
}
